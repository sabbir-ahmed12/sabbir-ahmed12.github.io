<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://sabbir-ahmed12.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://sabbir-ahmed12.github.io/" rel="alternate" type="text/html" hreflang="en" /><updated>2026-07-10T02:52:39+00:00</updated><id>https://sabbir-ahmed12.github.io/feed.xml</id><title type="html">blank</title><subtitle>This is Sabbir Ahmed&apos;s personal website.
</subtitle><entry><title type="html">Learn to Manage C++ Projects Using CMake Like a Pro (Part 1 - Building C++ Programs)</title><link href="https://sabbir-ahmed12.github.io/blog/2026/learn-cmake-part1/" rel="alternate" type="text/html" title="Learn to Manage C++ Projects Using CMake Like a Pro (Part 1 - Building C++ Programs)" /><published>2026-07-09T16:00:00+00:00</published><updated>2026-07-09T16:00:00+00:00</updated><id>https://sabbir-ahmed12.github.io/blog/2026/learn-cmake-part1</id><content type="html" xml:base="https://sabbir-ahmed12.github.io/blog/2026/learn-cmake-part1/"><![CDATA[<p>Building small C++ projects is straightforward, but large projects with multiple developers, external libraries, and cross-platform support require a more robust build system. CMake is a meta-build system that simplifies this process by describing your project’s structure and generating platform-specific build files (such as Visual Studio solutions or Makefiles). This course introduces the fundamentals of the C++ build process and gradually covers modern CMake techniques for developing large, cross-platform applications. Before using CMake, you’ll first learn about the tools involved in compiling and building C++ programs.</p>

<hr />

<h3 id="before-cmake-the-toolchain">Before CMake: The Toolchain</h3>

<p>CMake is like a project manager. It generates the necessary build files to build a project. It knows how to organize tasks, handle dependencies, and give the right instructions. But it doesn’t do the physical work itself. For that, it needs a team of skilled workers. In the world of C++, that team is called a toolchain.</p>

<p>A toolchain is a set of programming tools used to create a software product. For a C++ project, the essential components of a toolchain are:</p>

<ul>
  <li>
    <p><strong>A Compiler:</strong> A program that translates human-readable high level C++ source code into low-level machine code that a computer’s processor can understand.</p>
  </li>
  <li>
    <p><strong>A Linker:</strong> A program that takes the machine code from one or more sources (compiled files and any libraries) and combines them into a single executable program.</p>
  </li>
  <li>
    <p><strong>Standard Libraries:</strong> A collection of pre-compiled code that provides fundamental functionality. Examples include input/output with <code class="language-plaintext highlighter-rouge">std::cout</code>, and containers like <code class="language-plaintext highlighter-rouge">std::vector</code>.</p>
  </li>
</ul>

<p>CMake’s primary job is to find and control a toolchain. It needs to know which compiler to use, where to find libraries, and how to invoke the linker correctly for your target platform.</p>

<hr />

<h3 id="compiler">Compiler</h3>

<p>There are several C++ compilers available, but three stand out as the most widely used in the industry - GCC, Clang, and MSVC. CMake is designed to work with all of them. We’ll use GCC in our examples in this introductory chapter, but the choice isn’t that important. CMake will take care of the compiler interactions for us.</p>

<h4 id="gcc-gnu-compiler-collection">GCC (GNU Compiler Collection)</h4>

<p>GCC is one of the oldest and most established compilers. It originated as part of the GNU Project and is the standard compiler on most Linux systems. When you see the <code class="language-plaintext highlighter-rouge">g++</code> command, you’re using GCC’s C++ compiler. It’s known for its broad platform support and adherence to standards.</p>

<h4 id="clang">Clang</h4>

<p>Clang is a newer compiler front-end, developed as part of the LLVM project. It was designed to be faster and use less memory than GCC, but its most popular feature is its incredibly clear and helpful error messages.</p>

<p>Clang is the default compiler on macOS (via Apple’s toolchain) and is widely used on other platforms, including Windows and Linux. Its command-line interface, clang++, is designed to be a drop-in replacement for g++.</p>

<h4 id="msvc-microsoft-visual-c">MSVC (Microsoft Visual C++)</h4>

<p>If you’re a Windows developer, you’re likely familiar with Visual Studio. The compiler that powers it is MSVC (<code class="language-plaintext highlighter-rouge">cl.exe</code>). It’s a highly optimizing compiler with excellent integration into the Windows ecosystem.</p>

<p>While we’ll focus on setting up GCC or Clang for a consistent command-line experience across platforms, rest assured that CMake has first-class support for MSVC. In fact, installing Visual Studio is often the easiest way to get a complete C++ toolchain on Windows. We’ll primarilly use <code class="language-plaintext highlighter-rouge">g++</code> or <code class="language-plaintext highlighter-rouge">clang++</code> in our command-line examples, as their syntax is consistent across all operating systems.</p>

<hr />

<h3 id="setting-up-a-build-environment-on-windows">Setting Up a Build Environment on Windows</h3>

<h4 id="option-1-visual-studio-recommended-for-beginners">Option 1: Visual Studio (Recommended for Beginners)</h4>

<p>This is the batteries-included approach and easiest way to get started.</p>

<ol>
  <li>
    <p><strong>Download Visual Studio Installer:</strong> Go to the <a href="https://visualstudio.microsoft.com/downloads/">Visual Studio download page</a> and get the community edition, which is free for individuals and open-source projects.</p>
  </li>
  <li>
    <p><strong>Run the Installer:</strong> When the installer launches, it will show a list of workloads.</p>
  </li>
  <li>
    <p><strong>Select the C++ Workload:</strong> Check the box for “Desktop development with C++”. This single click will install everything you need: the MSVC compiler, the Windows SDK (which contains system headers and libraries), and a recent version of CMake.</p>
  </li>
  <li>
    <p><strong>Install:</strong> Click the install button and wait for the process to complete.</p>
  </li>
</ol>

<p>Once installed, you can access the compiler by opening the <strong>Developer Command Prompt for VS</strong>, which you can find in your Start Menu. This special terminal automatically configures the necessary environment variables.</p>

<div class="row mt-3">
    <div class="col-sm mt-3 mt-md-0">
        <figure>

  <picture>
    

    <!-- Fallback to the original file -->
    <img src="/assets/posts_img/2026_07_08_learn_cmake/vs-install-components.webp" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();" />

  </picture>

</figure>

    </div>
</div>

<h4 id="option-2-msys2-a-gnulinux-like-environment">Option 2: MSYS2 (A GNU/Linux-like Environment)</h4>

<p>If you prefer a command-line experience similar to Linux, using GCC on Windows is a great option. The best way to do this is with MSYS2.</p>

<ul>
  <li>
    <p><strong>Install MSYS2:</strong> Go to the <a href="https://www.msys2.org/">MSYS2 website</a> and follow their installation instructions. This will install a package manager called pacman, and some terminals we can use. We’ll use the MSYS2 UCRT64 terminal in this example</p>
  </li>
  <li>
    <p><strong>Update MSYS2:</strong> Open the MSYS2 UCRT64 terminal and run the following command to make sure everything is up to date:</p>
  </li>
</ul>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pacman -Syu
</code></pre></div></div>

<p>You may need to close and reopen the terminal and run it again to complete all updates.</p>

<ul>
  <li><strong>Install the Toolchain:</strong> Now, use pacman to install the MinGW-w64 toolchain, which includes GCC (g++), the linker, make, and other essential tools.</li>
</ul>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pacman -S --needed base-devel mingw-w64-ucrt-x86_64-toolchain
</code></pre></div></div>

<p>This command is likely to install a lot of packages, as there’s often a complex web of dependencies involved (that is, packages that require other packages to be installed)</p>

<p>That’s it! We’ll confirm everything is working correctly in the “Verifying Your Setup” section below.</p>

<blockquote class="block-tip">
  <h5 id="whats-the-deal-with-ucrt64">What’s the deal with ucrt64?</h5>

  <p>When you install a toolchain in MSYS2, you might see several options like mingw64, ucrt64, and clang64.</p>

  <ul>
    <li>ucrt64 uses the modern Universal C Runtime (UCRT) from Microsoft, which is the current standard. This is generally the one you want.</li>
    <li>mingw64 uses the older MSVCRT runtime. It’s there for compatibility with older projects.</li>
    <li>clang64 provides a Clang-based toolchain.</li>
  </ul>

  <p>For this course, sticking with the ucrt64 environment is the recommended path for GCC on Windows.</p>
</blockquote>

<h3 id="setting-up-a-build-environment-on-macos">Setting Up a Build Environment on macOS</h3>

<p>macOS makes this process very easy. The operating system ships with stubs for common developer tools, and trying to use them will prompt an installation.</p>

<ul>
  <li>
    <p><strong>Open the Terminal:</strong> You can find it in Applications/Utilities.</p>
  </li>
  <li>
    <p><strong>Run the Installer Command:</strong> Type the following command and press Enter.</p>
  </li>
</ul>

<div class="language-zsh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>xcode-select <span class="nt">--install</span>
</code></pre></div></div>

<ul>
  <li><strong>Confirm Installation:</strong> A dialog box will appear asking if you want to install the command line developer tools. Click “Install” and agree to the terms.</li>
</ul>

<p>This will download and install Apple’s Command Line Tools package, which includes clang, clang++, make, and other necessary utilities. It’s the official, Apple-supported way to get a C++ compiler on macOS.</p>

<blockquote class="block-tip">
  <h5 id="using-homebrew">Using Homebrew</h5>

  <p>If you’re a power user, you might prefer using the <a href="https://brew.sh/">Homebrew</a> package manager. Homebrew can install the very latest versions of GCC or Clang/LLVM, which might be newer than the versions provided by Apple.</p>

  <ul>
    <li>To install the latest GCC: <code class="language-plaintext highlighter-rouge">brew install gcc</code></li>
    <li>To install the latest Clang/LLVM: <code class="language-plaintext highlighter-rouge">brew install llvm</code></li>
  </ul>

  <p>If you do this, make sure you follow Homebrew’s instructions for setting your <code class="language-plaintext highlighter-rouge">PATH</code> so the system finds the Homebrew-installed versions first. For beginners, the <code class="language-plaintext highlighter-rouge">xcode-select</code> method is perfectly sufficient.</p>
</blockquote>

<h3 id="setting-up-a-build-environment-on-linux-debianubuntu">Setting Up a Build Environment on Linux (Debian/Ubuntu)</h3>

<p>Linux is a developer’s paradise. Getting a C++ toolchain is typically a single command away. The instructions here are for Debian-based distributions like Ubuntu, but the process is similar on other distros. This might involve using <code class="language-plaintext highlighter-rouge">dnf</code> on Fedora/CentOS or <code class="language-plaintext highlighter-rouge">pacman</code> on Arch.</p>

<ul>
  <li><strong>Update Your Package Lists:</strong> It’s always good practice to update your package manager’s index first.</li>
</ul>

<div class="language-zsh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt update
</code></pre></div></div>

<ul>
  <li><strong>Install build-essential:</strong> This is a special meta-package that installs everything you need for basic compilation, including gcc, g++, and make.</li>
</ul>

<div class="language-zsh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt <span class="nb">install </span>build-essential g++
</code></pre></div></div>

<p>That’s it! You now have a complete GCC toolchain ready to go. If you prefer to use Clang, you can install it just as easily:</p>

<div class="language-zsh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt <span class="nb">install </span>clang
</code></pre></div></div>

<hr />

<h3 id="verifying-setup">Verifying Setup</h3>

<p>Once you’ve completed the installation for your platform, it’s a good idea to verify that the compiler is correctly installed and accessible from your terminal. Open a new terminal window and try one of the following commands, based on the compiler you installed:</p>

<p>To check for GCC:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>g++ <span class="nt">--version</span>
</code></pre></div></div>

<p>It should print the version of gcc if it is installed correctly. If it says <code class="language-plaintext highlighter-rouge">command not found</code>, it means that the installation was not successful.</p>

<p>To check for Clang:</p>

<div class="language-zsh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>clang++ <span class="nt">--version</span>
</code></pre></div></div>

<p>If you’re on Windows using the Visual Studio Developer Command Prompt, check for MSVC:</p>

<pre><code class="language-cmd">cl
</code></pre>

<p>If you see output that includes a version number, congratulations! Your compiler is installed and ready.</p>

<hr />

<h3 id="an-example-of-manual-builds">An Example of Manual Builds</h3>

<p>Now that we have a working compiler, let’s see it in action. This will give us a taste of the manual build process and help us understand the problem that CMake was created to solve.</p>

<p>Let’s create a new file named <code class="language-plaintext highlighter-rouge">hello.cpp</code> and add the following code:</p>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="cp">
</span>
<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
  <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"Hello World!</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
  <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Let’s compile this program. We’re using the GCC compiler in our examples, and feel free to follow along if you are too, or adapt the steps to your own tools. But don’t feel like you need to follow along - eventually CMake will be handling the low-level compiler interactions for us,. We only need to understand the high level concepts.</p>

<p>To compile our <code class="language-plaintext highlighter-rouge">hello.cpp</code> file using <code class="language-plaintext highlighter-rouge">g++</code>, we’d run the following command in the same directory as the file:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>g++ hello.cpp <span class="nt">-o</span> hello
</code></pre></div></div>

<p>If you want to use <code class="language-plaintext highlighter-rouge">clang</code>, use the following command:</p>

<div class="language-zsh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>clang++ hello.cpp <span class="nt">-o</span> hello
</code></pre></div></div>

<p>Let’s break down the command:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">g++</code>: The C++ compiler program</li>
  <li><code class="language-plaintext highlighter-rouge">hello.cpp</code>: The input source file we want to compile.</li>
  <li><code class="language-plaintext highlighter-rouge">-o hello</code>: An option that specifies the name of the output file. If you omit this, the compiler will default to <code class="language-plaintext highlighter-rouge">a.out</code> on Linux/macOS or <code class="language-plaintext highlighter-rouge">a.exe</code> on Windows.</li>
</ul>

<p>If all goes well, there will be no output. Instead, you’ll find a new file in your directory: <code class="language-plaintext highlighter-rouge">hello</code> on Linux/macOS, or <code class="language-plaintext highlighter-rouge">hello.exe</code> on Windows. This is your executable program!</p>

<p>We can run our program directly from the command line simply using it’s name - <code class="language-plaintext highlighter-rouge">hello</code>, in this example. We may need to prepend <code class="language-plaintext highlighter-rouge">./</code> (as in <code class="language-plaintext highlighter-rouge">./hello</code>) to clarify to our terminal that the program we’re trying to run is in the current directory:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>./hello
</code></pre></div></div>

<p>It should print <code class="language-plaintext highlighter-rouge">Hello World!</code>.</p>

<blockquote class="block-danger">
  <h5 id="entry-point-not-found">Entry Point Not Found</h5>

  <p>When we compile our project for a specifc platform, it may not work correctly outside of that platform. For example, if we compile our project within the MSYS2 UCRT64 terminal, it is only likely to work when run from that same environment.</p>

  <p>If we run it from a different terminal, or just double-click its icon within Windows, it’s unlikely to run. We’ll learn how to create cross-platform projects later in the course. For now, we’ll just ensure we’re running the program in the same environment it was compiled in.</p>
</blockquote>

<hr />

<h3 id="building-multi-file-projects">Building Multi-File Projects</h3>

<p>In this case, our program is a single source file, which makes it fairly easy to build. But let’s create a project with two source files - <code class="language-plaintext highlighter-rouge">greeter.cpp</code> and <code class="language-plaintext highlighter-rouge">main.cpp</code>, and one header file - <code class="language-plaintext highlighter-rouge">greeter.h</code>. Now write the following lines in the respective files:</p>

<p><code class="language-plaintext highlighter-rouge">geeter.h</code>:</p>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#pragma once
#include</span> <span class="cpf">&lt;string&gt;</span><span class="cp">
</span>
<span class="kt">void</span> <span class="nf">greet</span><span class="p">(</span><span class="k">const</span> <span class="n">std</span><span class="o">::</span><span class="n">string</span><span class="o">&amp;</span> <span class="n">name</span><span class="p">);</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">greeter.cpp</code>:</p>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">"greeter.h"</span><span class="cp">
#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="cp">
</span>
<span class="kt">void</span> <span class="nf">greet</span><span class="p">(</span><span class="k">const</span> <span class="n">std</span><span class="o">::</span><span class="n">string</span><span class="o">&amp;</span> <span class="n">name</span><span class="p">)</span> <span class="p">{</span>
  <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"Hello, "</span> <span class="o">&lt;&lt;</span> <span class="n">name</span> <span class="o">&lt;&lt;</span> <span class="s">"!</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">main.cpp</code>:</p>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">"greeter.h"</span><span class="cp">
</span>
<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
  <span class="n">greet</span><span class="p">(</span><span class="s">"second file"</span><span class="p">);</span>
  <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p>To build this, we now need to give both of our source files to the compiler:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>g++ main.cpp greeter.cpp <span class="nt">-o</span> app
</code></pre></div></div>

<p>For <code class="language-plaintext highlighter-rouge">clang</code> users:</p>

<div class="language-zsh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>clang++ main.cpp greeter.cpp <span class="nt">-o</span> app
</code></pre></div></div>

<p>We don’t need to provide our header file in this example, as its contents are already getting inserted into <code class="language-plaintext highlighter-rouge">main.cpp</code> because of the <code class="language-plaintext highlighter-rouge">#include</code> directive. We’ll talk more about header files in the next lesson.</p>

<p>If our program compiled, we can run it in the usual way:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>./app
</code></pre></div></div>

<p>It should print <code class="language-plaintext highlighter-rouge">Hello, second file!</code>.</p>

<p>Again, this tiny project with its two source files is manageable. But what if we had 20 files? Or 200? What if some files needed special compiler flags for optimization, while others needed flags for debugging? What if we wanted to build on Windows with MSVC, which uses completely different commands and flags?</p>

<p>The complexity spirals out of control. Manually maintaining build commands or writing platform-specific scripts (like Makefiles or batch files) is a tedious and error-prone nightmare.</p>

<p><strong><em>This is the problem CMake solves.</em></strong></p>

<blockquote class="block-tip">
  <h5 id="but-my-ide-handles-this-already">But My IDE Handles This Already?</h5>

  <p>You might be thinking, “I use Visual Studio, and when I add a file, it just works. Why do I need another tool?” It’s a great question.</p>

  <p>IDEs like Visual Studio have their own build systems based on Solution (<code class="language-plaintext highlighter-rouge">.sln</code>) and Project (<code class="language-plaintext highlighter-rouge">.vcxproj</code>) files. Behind the scenes, these files are automatically updated as you configure your project in your IDE, such as adding new files.</p>

  <p>This is convenient, particularly for beginners, but they lock you into a specific ecosystem.</p>

  <p>Here’s why CMake is often a better choice, especially for large or collaborative projects:</p>

  <ul>
    <li><strong>Portability is King:</strong> The primary advantage of CMake is portability. A Visual Studio solution file (<code class="language-plaintext highlighter-rouge">.sln</code>) is specific to Windows and Visual Studio. You can’t give it to a colleague on a Mac using Xcode or a Linux user with a command-line setup. However, CMake configuration files (<code class="language-plaintext highlighter-rouge">CMakeLists.txt</code>) are universal. The same file can generate a native Visual Studio project, an Xcode project, or a Linux Makefile without any changes.</li>
    <li><strong>A Single Source of Truth:</strong> With CMake, your <code class="language-plaintext highlighter-rouge">CMakeLists.txt</code> file is the single, authoritative description of your project. Everyone on the team using and updating the same build definition is important for consistency. It avoids the “works on my machine” problems.</li>
    <li><strong>Automation and CI/CD:</strong> CMake is designed for automation. Because it’s a command-line tool, it’s perfect for Continuous Integration (CI) pipelines on build servers like GitHub Actions or Jenkins. You can’t easily run the Visual Studio GUI on a build server, but you can easily run <code class="language-plaintext highlighter-rouge">cmake</code> and <code class="language-plaintext highlighter-rouge">cmake --build</code> commands in a script. We’ll cover build automation and its advantages in a dedicated chapter later in the course.</li>
  </ul>

  <p>Ultimately, an IDE’s project file mixes your project’s structure with IDE-specific settings.</p>

  <p>CMake separates these concerns: your CMakeLists.txt file is a pure, portable description of your project. This gives you the freedom to build it anywhere, with any tool, without being locked into a single vendor’s ecosystem.</p>
</blockquote>

<hr />

<h3 id="whats-next">What’s Next?</h3>

<p>We’ve successfully prepared our development environment and gotten a small taste of the build process. We have our tools, and we’ve seen a hint of the complexity that lies ahead.</p>

<p>Now, we’re ready to peel back the layers. In the next lesson, we will go deeper into what actually happens when you run that <code class="language-plaintext highlighter-rouge">g++</code> command.</p>

<p>We’ll explore the C++ compilation pipeline step-by-step, from preprocessing and compilation to the role of the linker. Understanding these fundamentals is the key to mastering any build system, especially <code class="language-plaintext highlighter-rouge">CMake</code>.</p>

<hr />

<h3 id="references">References</h3>

<p>[1] STUDYPLAN.dev (https://www.studyplan.dev/)</p>

<p>[2] GeeksForGeeks (https://www.geeksforgeeks.org/)</p>

<p>[3] CMake.org (https://cmake.org/getting-started/)</p>]]></content><author><name></name></author><category term="tutorial" /><category term="C++" /><summary type="html"><![CDATA[Learn about the C++ toolchain]]></summary></entry><entry><title type="html">Learn to Manage C++ Projects Using CMake Like a Pro (Part 2 - Understanding the Compilation Pipeline)</title><link href="https://sabbir-ahmed12.github.io/blog/2026/learn-cmake-part2/" rel="alternate" type="text/html" title="Learn to Manage C++ Projects Using CMake Like a Pro (Part 2 - Understanding the Compilation Pipeline)" /><published>2026-07-09T11:04:00+00:00</published><updated>2026-07-09T11:04:00+00:00</updated><id>https://sabbir-ahmed12.github.io/blog/2026/learn-cmake-part2</id><content type="html" xml:base="https://sabbir-ahmed12.github.io/blog/2026/learn-cmake-part2/"><![CDATA[<p>C++ program building is a multi step process where source code turns into an executable. Each step transforms the code closer to the machine language.</p>

<p>In this lesson, we’ll explore the early stages of that pipeline: taking C++ source and header files through preprocessing, compilation, and assembly to produce object files.</p>

<div class="row mt-3">
    <div class="col-sm mt-3 mt-md-0">
        <figure>

  <picture>
    

    <!-- Fallback to the original file -->
    <img src="/assets/posts_img/2026_07_08_learn_cmake/part2-compilation-steps.webp" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();" />

  </picture>

</figure>

    </div>
</div>

<p>These object files contain the machine code output for our program, and serve as the input for the next stage (linking) to create the final executable.</p>

<p>By understanding this compilation pipeline, you’ll gain insight into why we structure C++ projects with header (<code class="language-plaintext highlighter-rouge">.h/.hpp</code>) and source (<code class="language-plaintext highlighter-rouge">.cpp</code>) files, why certain errors like missing symbols or multiple definitions occur, and how tools like build systems (and later CMake) orchestrate these steps.</p>

<p>We’ll cover translation units, macro expansion, include directives, the generation of <code class="language-plaintext highlighter-rouge">.obj/.o</code> files, and the role of the assembler in converting human-readable assembly to machine code.</p>

<p>The final linking step, which combines object files into an executable or library, will be discussed in the next lesson.</p>

<hr />

<h3 id="source-files-headers-and-translation-units">Source Files, Headers, and Translation Units</h3>

<p>C++ programs are typically spread across multiple files.</p>

<ul>
  <li>
    <p>Source files, often with <code class="language-plaintext highlighter-rouge">.cpp</code> or <code class="language-plaintext highlighter-rouge">.cc</code> extensions, contain function and class definitions - the actual code logic.</p>
  </li>
  <li>
    <p>Header files, usually with <code class="language-plaintext highlighter-rouge">.h</code> or <code class="language-plaintext highlighter-rouge">.hpp</code> extensions, typically focus on declations - interfaces, function prototypes, class definitions without method bodies, constants etc.</p>
  </li>
</ul>

<p>This separation is a long-standing C++ convention that helps organize code and manage compilation dependencies. If you’re not familiar, you can learn more about <a href="#">header files</a> and their rationale.</p>

<p>When you compile a C++ program, you usually compile each source file independently. The compiler doesn’t immediately know about all parts of your program - it only looks at one source file at a time, plus whatever that source brings in via <code class="language-plaintext highlighter-rouge">#include</code>.</p>

<p>This is where the concept of a translation unit comes in. A translation unit is essentially one source file after it has been preprocessed, meaning all the headers it includes are literally inserted and all macros are expanded.</p>

<div class="alert alert-info" role="alert">
<h5> Translation Unit</h5>

<p>A translation unit is the input to the compiler's compile step. It consists of a C++ source file after all of the preprocessor directives have been handled.</p>

<p>The translation unit is the complete, self-contained C++ code that the compiler uses to generate a single object file.</p>
</div>

<p>Each source file results in one translation unit and yields one object file when compiled. If you have, say, <code class="language-plaintext highlighter-rouge">main.cpp</code> and <code class="language-plaintext highlighter-rouge">util.cpp</code> in your project, the compiler will produce something like <code class="language-plaintext highlighter-rouge">main.o</code> and <code class="language-plaintext highlighter-rouge">util.o</code> separately (or <code class="language-plaintext highlighter-rouge">main.obj</code> and <code class="language-plaintext highlighter-rouge">util.obj</code> on Windows). It’s then the linker’s job to combine those object files, but we’ll get to that later.</p>

<p>The key point is that the only way code from one file is seen while compiling another is either via an <code class="language-plaintext highlighter-rouge">#include</code> directive. <a href="#">C++20 modules</a> are another way but, because they’re not yet widely supported or used, we’ll ignore those for now.</p>

<p>So, if <code class="language-plaintext highlighter-rouge">main.cpp</code> needs something from <code class="language-plaintext highlighter-rouge">util.cpp</code>, it must include a header that describes what is in <code class="language-plaintext highlighter-rouge">util.cpp</code>. Otherwise, the compiler processing <code class="language-plaintext highlighter-rouge">main.cpp</code> has no knowledge of <code class="language-plaintext highlighter-rouge">util.cpp</code>’s contents. This is why missing includes lead to compile errors like “identifier not found” if you forgot to include a header with a needed declaration.</p>

<hr />

<h3 id="preprocessing-stage">Preprocessing Stage</h3>

<p>The first stage of building a C++ program is preprocessing. The preprocessor is a tool that runs before the compiler’s proper analysis of the C++ code.</p>

<p>It works at the textual level, performing simple text substitutions and conditional removals according to directives that start with <code class="language-plaintext highlighter-rouge">#</code>. These changes broadly fall into three categories: include resolution, macro expansion, and conditional compilation.</p>

<h4 id="include-resolution">Include Resolution</h4>

<p>When it encounters <code class="language-plaintext highlighter-rouge">#include &lt;...&gt;</code> or <code class="language-plaintext highlighter-rouge">#include "..."</code>, it will literally replace that line with the contents of the referenced file, which is usually a header file.</p>

<p>In other words, it pastes the header file’s code into the source. This is recursive - if that header includes other headers, those get expanded too. By the end, it’s as if you wrote one big file containing your source and all the headers it needs.</p>

<p>For instance, if <code class="language-plaintext highlighter-rouge">main.cpp</code> has <code class="language-plaintext highlighter-rouge">#include "util.h"</code>, the preprocessor will insert everything from <code class="language-plaintext highlighter-rouge">util.h</code> into <code class="language-plaintext highlighter-rouge">main.cpp</code>’s code. It will recursively do the same for any includes inside <code class="language-plaintext highlighter-rouge">util.h</code>, and so on.</p>

<h4 id="macro-expansion">Macro Expansion</h4>

<p>The preprocessor handles <code class="language-plaintext highlighter-rouge">#define</code> macros. A macro definition like <code class="language-plaintext highlighter-rouge">#define MAX_SIZE 100</code> tells the preprocessor to replace every occurrence of <code class="language-plaintext highlighter-rouge">MAX_SIZE</code> in the code with <code class="language-plaintext highlighter-rouge">100</code> before compilation. Function-like macros can also take arguments, for example:</p>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#define SQUARE(x) ((x) * (x))
</span><span class="kt">int</span> <span class="n">a</span> <span class="o">=</span> <span class="n">SQUARE</span><span class="p">(</span><span class="mi">5</span><span class="p">);</span>
</code></pre></div></div>

<p>Here, the preprocessor will replace <code class="language-plaintext highlighter-rouge">SQUARE(5)</code> with <code class="language-plaintext highlighter-rouge">((5) * (5))</code> wherever it appears. The compiler never sees <code class="language-plaintext highlighter-rouge">SQUARE</code> - by the time we get to actual compilation, that symbol is expanded into its replacement code.</p>

<p>This can lead to pitfalls if not carefully used - e.g., macro expansions don’t follow normal function call rules - but that’s beyond our scope here. Check the <a href="#">preprocessor definitions</a> lesson for more about writing macros.</p>

<h4 id="conditional-compilation">Conditional Compilation</h4>

<p>Directives like <code class="language-plaintext highlighter-rouge">#if</code>, <code class="language-plaintext highlighter-rouge">#ifdef</code>, <code class="language-plaintext highlighter-rouge">#ifndef</code>, and <code class="language-plaintext highlighter-rouge">#endif</code> let the preprocessor include or exclude parts of code based on condition.</p>

<p>For instance, you might wrap debug code in <code class="language-plaintext highlighter-rouge">#ifdef DEBUG</code> so that it’s included only when you define a <code class="language-plaintext highlighter-rouge">DEBUG</code> macro. The preprocessor evaluates these conditions and strips out any code in <code class="language-plaintext highlighter-rouge">#if</code> blocks that evaluate to false. For example:</p>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#define DEBUG 1
</span>
<span class="c1">// ...</span>

<span class="cp">#if DEBUG
</span>  <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"Debug mode</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
<span class="cp">#endif
</span></code></pre></div></div>

<p>Here, if <code class="language-plaintext highlighter-rouge">DEBUG</code> is 1 (true), the line will remain; if we had <code class="language-plaintext highlighter-rouge">#define DEBUG 0</code> or no definition, the <code class="language-plaintext highlighter-rouge">std::cout &lt;&lt; ...</code> line would be removed entirely from the code seen by the compiler.</p>

<p>After preprocessing, what we have is the translation unit mentioned earlier - a single, combined source file with no <code class="language-plaintext highlighter-rouge">#include</code> or <code class="language-plaintext highlighter-rouge">#define</code> or <code class="language-plaintext highlighter-rouge">#if</code> directives anymore. All those have been acted upon:</p>

<ul>
  <li>Every include file’s contents is present - often many thousands of lines once you include standard headers like <code class="language-plaintext highlighter-rouge">&lt;iostream&gt;</code>.</li>
  <li>Every macro is expanded or eliminated.</li>
  <li>Every conditional block is either kept or cut out.</li>
</ul>

<hr />

<h3 id="viewing-preprocessor-output">Viewing Preprocessor Output</h3>

<p>You can try running the preprocessor on a small program to see what it produces. Save the following in <code class="language-plaintext highlighter-rouge">example.cpp</code>:</p>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span><span class="cpf">&lt;iostream&gt;</span><span class="cp">
#define MESSAGE "Hello, world!"
</span>
<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="c">#if 0
        std::cout &lt;&lt; "Debug mode\n";
    #endif
</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="n">MESSAGE</span><span class="p">;</span>
    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Now run the command below to get the preprocessor output.</p>

<p>For GCC:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>g++ <span class="nt">-E</span> <span class="nt">-P</span> example.cpp <span class="nt">-o</span> example.i
</code></pre></div></div>

<p>For Clang:</p>

<div class="language-zsh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>clang++ <span class="nt">-E</span> <span class="nt">-P</span> example.cpp <span class="nt">-o</span> example.i
</code></pre></div></div>

<p>In this case, <code class="language-plaintext highlighter-rouge">-E</code> asks <code class="language-plaintext highlighter-rouge">g++</code> to only run the preprocessor, and <code class="language-plaintext highlighter-rouge">-P</code> asks it to exclude line markers from the output. We’ll explain line markers soon. If you want to learn more about different flags, you can try running <code class="language-plaintext highlighter-rouge">g++ --help</code> or <code class="language-plaintext highlighter-rouge">clang++ --help</code>.</p>

<p>After running the command, we can open <code class="language-plaintext highlighter-rouge">example.i</code> in a text editor. We should see that:</p>

<ul>
  <li>
    <p>The line <code class="language-plaintext highlighter-rouge">#include &lt;iostream&gt;</code> has been replaced by a huge amount of code - tens of thousands of lines. This is the entire contents of the <code class="language-plaintext highlighter-rouge">iostream</code> header and all headers it includes. The code we wrote in our original source file will be at the bottom of the file, below all of this stuff.</p>
  </li>
  <li>
    <p>The macro <code class="language-plaintext highlighter-rouge">MESSAGE</code> is gone, and its occurrences have been replaced with <code class="language-plaintext highlighter-rouge">"Hello, world!"</code>.</p>
  </li>
  <li>
    <p>The block under <code class="language-plaintext highlighter-rouge">#if 0</code> is completely removed - it won’t appear in the output at all.</p>
  </li>
</ul>

<p>This can be an eye-opening experiment to understand how much work the preprocessor does and why the resulting translation unit is often much larger than your initial source file.</p>

<p>Once preprocessing is done, we have our translation unit ready. Now the heavy lifting begins: the compiler will translate this (potentially enormous) code into machine code. That happens in the next stage.</p>

<div class="alert alert-info" role="alert">
<h5>Line Markers</h5>
<p>Line markers are added to translation units to help the compiler and debuggers understand where each line of code in the translation unit originally came from.</p>

<p>For example, if a line of code was originally in <code>SomeHeaderFile.h</code> and got added to our <code>example.cpp</code> translation unit through an <code>#include</code> directive, the line marker keeps track of that. It also keeps track of where the line was within that original header file - that is, it's line number within <code>SomeHeaderFile.h</code> before the preprocessor modified it.</p>

<p>Line markers are not required - the compiler can complete it's work perfectly fine without them. However, if the compiler encounters an error in the translation unit, it can use the line marker to report the original source of the error. For example, it lets the compiler report that the error came from line 15 of <code>SomeHeaderFile.h</code> rather than line 25,461 of some translation unit. This makes it easier for us to find and fix the original source of the problem.</p>
</div>

<hr />

<h3 id="compilation-to-object-code">Compilation to Object Code</h3>
<p>After preprocessing, the compiler proper takes over. This stage is what we usually think of as “compiling”: the compiler reads the preprocessed C++ code - the translation unit - and converts it to machine code instructions.</p>

<p>However, it doesn’t immediately make a full program out of it - it produces an object file containing the machine code for just this translation unit.</p>

<h4 id="object-files">Object Files</h4>
<p>What exactly is an object file? It’s a file that contains binary machine code and some extra information needed for later linking. The machine code in an object file corresponds to the functions and code in your source, but if your code calls a function that’s defined in another file, the call is left as a placeholder that the linker will fill in.</p>

<p>Think of an object file as “half a puzzle” - it has pieces of the program, but references to other pieces are not resolved yet.</p>

<p>For example, if <code class="language-plaintext highlighter-rouge">main.cpp</code> calls a function <code class="language-plaintext highlighter-rouge">utilFunc()</code> that’s defined in <code class="language-plaintext highlighter-rouge">util.cpp</code>, when compiling <code class="language-plaintext highlighter-rouge">main.cpp</code> the compiler will include a call instruction to <code class="language-plaintext highlighter-rouge">utilFunc</code> but mark it as an external symbol. The actual address for <code class="language-plaintext highlighter-rouge">utilFunc</code> isn’t known at this point and will be fixed when linking <code class="language-plaintext highlighter-rouge">main.o</code> with <code class="language-plaintext highlighter-rouge">util.o</code>.</p>

<p>Object files typically have the extension <code class="language-plaintext highlighter-rouge">.o</code> on Unix-like systems and <code class="language-plaintext highlighter-rouge">.obj</code> on Windows.</p>

<p>They are often in a format like <code class="language-plaintext highlighter-rouge">ELF</code> (on Linux/macOS) or <code class="language-plaintext highlighter-rouge">COFF/PE</code> (on Windows), which specifies how machine code and data are stored, and includes tables for unresolved symbols - things to link later - and other metadata. You generally don’t need to inspect object files directly as they’re not meant to be human-readable, but it’s useful to know they exist and are the output of the compilation stage.</p>

<p>During this stage, the compiler will perform several tasks:</p>

<ul>
  <li>
    <p><strong>Syntax and Semantic Analysis:</strong> It checks that the code is valid C++ now that macros have been expanded. It ensures types match, variables are declared, etc.</p>
  </li>
  <li>
    <p><strong>Code Generation:</strong> It translates C++ constructs to equivalent lower-level operations. For instance, a simple arithmetic operation or a loop gets translated into a sequence of machine instructions that achieve the same effect.</p>
  </li>
  <li>
    <p><strong>Optimization:</strong> If you have optimization enabled - that is, using <code class="language-plaintext highlighter-rouge">O2</code> or <code class="language-plaintext highlighter-rouge">O3</code> flags in GCC/Clang, or default “Release” mode in MSVC - then the compiler will try to improve the machine code. It can do this by removing unused code, inlining functions, unrolling loops, etc. This doesn’t change the program’s behavior but can make it faster or smaller.</p>
  </li>
  <li>
    <p><strong>Generate Object File:</strong> Finally, the compiler writes out the machine code and data to the object file, along with symbol tables listing things like “this object provides function X” or “this object needs a definition for function Y from elsewhere.”</p>
  </li>
</ul>

<p>It’s at this stage you encounter compile-time errors if something is wrong with your C++ code’s structure or rules. For instance, forgetting a semicolon, calling a function with the wrong number of arguments, or violating C++ syntax will cause errors here.</p>

<div class="alert alert-info" role="alert">
<h5>Viewing Object Files</h5>

<p>Typically, you don't run the compiler manually for each stage; you use a compiler driver like the <code>g++</code> or <code>clang++</code> command, or Visual Studio's build system. This runs all necessary stages for you.
</p>

<p>For instance, invoking <code>g++ main.cpp util.cpp</code> will internally preprocess and compile both, producing <code>main.o</code> and <code>util.o</code>, then link them.</p>

<p>If you only want to compile to object files without linking, you can use a flag like <code>-c</code>. For example, running <code>g++ -c main.cpp</code> should create a <code>main.o</code> or <code>main.obj</code> file.</p>

<p>These files are not meant to be human-readable, but we'll explain what they contain in the next section.</p>
</div>

<hr />

<h3 id="the-assembler-and-machine-code">The Assembler and Machine Code</h3>

<p>You might wonder how exactly the human-readable C++ source turns into the binary 0s and 1s that the CPU executes. The compiler doesn’t directly write binary machine code into the object file in many cases - it often goes through an intermediate step: assembly language.</p>

<p>Assembly language is a human-readable representation of machine code. Each CPU instruction like “add two registers” or “jump to address if zero” has a textual mnemonic (like <code class="language-plaintext highlighter-rouge">ADD</code> or <code class="language-plaintext highlighter-rouge">JZ</code>) and operands (register names, immediate values, memory addresses).</p>

<p>Assembly is specific to the processor architecture. For example, x86-64 assembly differs from ARM assembly. However, it’s much simpler than C++ as it’s basically a direct mapping to machine instructions.</p>

<p>In many compilers, the process looks like this:</p>

<ol>
  <li>
    <p>The compiler’s code generation phase writes out an assembly file, often with a <code class="language-plaintext highlighter-rouge">.s</code> extension, from the C++ source.</p>
  </li>
  <li>
    <p>An assembler program then reads the assembly file and produces an object file with actual machine code bytes.</p>
  </li>
</ol>

<p>This two-step approach has historical and practical reasons.</p>

<p>Historically, it was simpler to implement the complex high-level language translation in the compiler and then delegate the well-understood task of turning assembly into object code to an assembler.</p>

<p>Practically, it also allows developers to inspect the assembly output for optimization or debugging. Some compilers still make it easy to get the assembly output with a flag like <code class="language-plaintext highlighter-rouge">-S</code>.</p>

<p>Additionally, using an assembler abstracts away the details of the object file format and relocation for the compiler writers - the assembler knows how to produce a correct <code class="language-plaintext highlighter-rouge">.o/.obj</code> file for the platform, so the compiler can focus on generating correct assembly.</p>

<h4 id="viewing-assembly-output">Viewing Assembly Output</h4>
<p>Most compilers include an option to stop after compilation and output the assembly code, rather than an object file or executable. In GCC/Clang, this can be done using the uppercase <code class="language-plaintext highlighter-rouge">-S</code> flag:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>g++ <span class="nt">-S</span> some_file.cpp
<span class="c"># Or,</span>
clang++ <span class="nt">-S</span> some_file.cpp
</code></pre></div></div>
<p>Once it finishes, you’ll see a file saved as <code class="language-plaintext highlighter-rouge">some_file.s</code> in the same directory. You can read the file from terminal using <code class="language-plaintext highlighter-rouge">cat some_file.s</code> and you may notice some memory locations called registers and some other commands like <code class="language-plaintext highlighter-rouge">movq</code>, <code class="language-plaintext highlighter-rouge">movl</code> etc. Understanding assembly is not required, but there are many free resources available online. <a href="https://gpfault.net/posts/asm-tut-0.txt.html">Check this</a> four part series to understand assembly code.</p>

<p>For now, the key point is that each high-level C++ statement corresponds to a handful of assembly instructions. The compiler will translate each of our C++ lines into this assembly, and the assembler will then translate these assembly instructions into machine code. This will be output as our object file.</p>

<p>It’s worth noting that not all compilers invoke a separate assembler program these days.</p>

<p>For instance, MSVC on Windows generates object files directly, and LLVM’s back-end can directly emit machine code without writing an assembly file.</p>

<p>But conceptually, even in those cases, there is an “assembly generation phase” internally followed by an encoding to object file format. The result is the same: we get an object file with machine code.</p>

<p>In the next lesson, we’ll learn about object files and the linking process.</p>

<p><strong><em>Note: This cmake tutorial series will be mostly copied contents from studyplan.dev website. I’ve written them as I was going through all the chapters so that I can revise the materials later if needed.</em></strong></p>]]></content><author><name></name></author><category term="tutorial" /><category term="C++" /><summary type="html"><![CDATA[Steps of the C++ compilation process, from source code to machine code.]]></summary></entry><entry><title type="html">Function Pointer in C++</title><link href="https://sabbir-ahmed12.github.io/blog/2026/function-pointer-in-c++/" rel="alternate" type="text/html" title="Function Pointer in C++" /><published>2026-06-27T04:57:00+00:00</published><updated>2026-06-27T04:57:00+00:00</updated><id>https://sabbir-ahmed12.github.io/blog/2026/function-pointer-in-c++</id><content type="html" xml:base="https://sabbir-ahmed12.github.io/blog/2026/function-pointer-in-c++/"><![CDATA[<h1 id="what-is-function-pointer">What is Function Pointer?</h1>
<p>In C++, pointers store memory address. While they are commonly used to store memory address of variables or objects, they can also store the address of a function. A function pointer allows a program to call a function indirectly or pass a function as an argument, enabling flexible and dynamic behavior.</p>

<p>Consider the example below to find the address of a function:</p>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span><span class="cpf">&lt;iostream&gt;</span><span class="cp">
</span>
<span class="n">ing</span> <span class="nf">getNumber</span><span class="p">(</span><span class="kt">int</span> <span class="n">num</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">return</span> <span class="n">num</span><span class="p">;</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="n">getNumber</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>
    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>
<p>Note that the function is not called here, we just put the function name without <code class="language-plaintext highlighter-rouge">()</code>.</p>

<p>So, a function pointer is a pointer variable that stores the address of a function with specific return type and parameter list.</p>

<h2 id="how-to-create-a-function-pointer-and-invoke-a-function-using-function-pointer">How to Create a Function Pointer and Invoke a Function Using Function Pointer?</h2>
<p>Consider the example below where we create the <code class="language-plaintext highlighter-rouge">add</code> function. It takes two integers and return the sum of the integers. To create a function pointer of that function we need to follow the synax below:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return_type(*functionPtr)(parameter_type);
</code></pre></div></div>
<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span><span class="cpf">&lt;iostream&gt;</span><span class="cp">
</span>
<span class="kt">int</span> <span class="nf">add</span><span class="p">(</span><span class="kt">int</span> <span class="n">num1</span><span class="p">,</span> <span class="kt">int</span> <span class="n">num2</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">return</span> <span class="n">num1</span> <span class="o">+</span> <span class="n">num2</span><span class="p">;</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="c1">// Creating a pointer to the add function</span>
    <span class="kt">int</span><span class="p">(</span><span class="o">*</span><span class="n">addFuncPtr</span><span class="p">)(</span><span class="kt">int</span><span class="p">,</span> <span class="kt">int</span><span class="p">)</span> <span class="o">=</span> <span class="n">add</span><span class="p">;</span>

    <span class="c1">// Invoke using the function pointer</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="n">addFuncPtr</span><span class="p">(</span><span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">)</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>

    <span class="c1">// Invoke the function directly</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="n">add</span><span class="p">(</span><span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">)</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>

    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="applications-of-function-pointer">Applications of Function Pointer:</h2>
<h3 id="passing-functions-as-argument-callback-function">Passing functions as argument (Callback Function)</h3>
<p>If you’re familiar with Python or Javascript, you may know that you can pass a function as an argument of another function. That’s one of reasons why functions are called first class citizen or object in Python. We can implement that using function pointer in C++. Consider the example below where we want to write a sort function. Depending on the function we pass as argument to the sort function, it sorts a list of number either in ascending or descending order.</p>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span><span class="cpf">&lt;iostream&gt;</span><span class="cp">
#include</span><span class="cpf">&lt;vector&gt;</span><span class="cp">
</span>
<span class="cm">/*
* Sort into ascending oder
*/</span>
<span class="kt">bool</span> <span class="nf">ascendingCompare</span><span class="p">(</span><span class="kt">int</span> <span class="n">a</span><span class="p">,</span> <span class="kt">int</span> <span class="n">b</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">return</span> <span class="n">a</span> <span class="o">&lt;</span> <span class="n">b</span><span class="p">;</span>
<span class="p">}</span>

<span class="cm">/*
* Sort into descending order
*/</span>
<span class="kt">bool</span> <span class="nf">descendingCompare</span><span class="p">(</span><span class="kt">int</span> <span class="n">a</span><span class="p">,</span> <span class="kt">int</span> <span class="n">b</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">return</span> <span class="n">a</span> <span class="o">&gt;</span> <span class="n">b</span><span class="p">;</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">sort</span><span class="p">(</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;&amp;</span> <span class="n">intVector</span><span class="p">,</span> <span class="kt">bool</span><span class="p">(</span><span class="o">*</span><span class="n">compareFuncPtr</span><span class="p">)(</span><span class="kt">int</span><span class="p">,</span> <span class="kt">int</span><span class="p">))</span> <span class="p">{</span>
    <span class="k">for</span><span class="p">(</span><span class="kt">int</span> <span class="n">startIdx</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">startIdx</span> <span class="o">&lt;</span> <span class="n">intVector</span><span class="p">.</span><span class="n">size</span><span class="p">();</span> <span class="n">startIdx</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>

        <span class="kt">int</span> <span class="n">bestIdx</span> <span class="o">=</span> <span class="n">startIdx</span><span class="p">;</span>

        <span class="k">for</span><span class="p">(</span><span class="kt">int</span> <span class="n">currentIdx</span><span class="o">=</span><span class="n">startIdx</span><span class="o">+</span><span class="mi">1</span><span class="p">,</span> <span class="n">currentIdx</span> <span class="o">&lt;</span> <span class="n">intVector</span><span class="p">.</span><span class="n">size</span><span class="p">();</span> <span class="n">currentIdx</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
            <span class="k">if</span><span class="p">(</span><span class="n">compareFuncPtr</span><span class="p">(</span><span class="n">intVector</span><span class="p">[</span><span class="n">currentIdx</span><span class="p">],</span> <span class="n">intVector</span><span class="p">[</span><span class="n">bestIdx</span><span class="p">]))</span> <span class="p">{</span>
                <span class="n">bestIdx</span> <span class="o">=</span> <span class="n">currentIdx</span><span class="p">;</span>
            <span class="p">}</span>
        <span class="p">}</span>
        <span class="n">swap</span><span class="p">(</span><span class="n">intVector</span><span class="p">[</span><span class="n">startIdx</span><span class="p">],</span> <span class="n">intVector</span><span class="p">[</span><span class="n">bestIdx</span><span class="p">]);</span>
    <span class="p">}</span>
<span class="p">}</span>

<span class="cm">/*
* Prints the numbers in a vector
*/</span>
<span class="kt">void</span> <span class="nf">printVector</span><span class="p">(</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;&amp;</span> <span class="n">intVector</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">for</span><span class="p">(</span><span class="kt">int</span> <span class="n">i</span><span class="o">=</span><span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">intVector</span><span class="p">.</span><span class="n">size</span><span class="p">();</span> <span class="n">i</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="n">intVector</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">&lt;&lt;</span> <span class="s">" "</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span> <span class="n">numbers</span> <span class="o">=</span> <span class="p">{</span><span class="mi">12</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">5</span><span class="p">,</span> <span class="mi">6</span><span class="p">,</span> <span class="mi">10</span><span class="p">,</span> <span class="mi">2</span><span class="p">};</span>
    <span class="n">printVector</span><span class="p">(</span><span class="n">numbers</span><span class="p">);</span>
    <span class="kt">bool</span><span class="p">(</span><span class="o">*</span><span class="n">functionPtr</span><span class="p">)(</span><span class="kt">int</span><span class="p">,</span> <span class="kt">int</span><span class="p">)</span> <span class="o">=</span> <span class="n">ascendingCompare</span><span class="p">;</span>
    <span class="n">sort</span><span class="p">(</span><span class="n">numbers</span><span class="p">,</span> <span class="n">ascendingCompare</span><span class="p">);</span>
    <span class="n">printVector</span><span class="p">(</span><span class="n">numbers</span><span class="p">);</span>

    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>]]></content><author><name></name></author><category term="tutorial" /><category term="C++" /><summary type="html"><![CDATA[Learn about function pointer and it's application]]></summary></entry><entry><title type="html">Pointer in C/C++</title><link href="https://sabbir-ahmed12.github.io/blog/2026/pointer-in-c-and-c++/" rel="alternate" type="text/html" title="Pointer in C/C++" /><published>2026-06-27T04:57:00+00:00</published><updated>2026-06-27T04:57:00+00:00</updated><id>https://sabbir-ahmed12.github.io/blog/2026/pointer-in-c-and-c++</id><content type="html" xml:base="https://sabbir-ahmed12.github.io/blog/2026/pointer-in-c-and-c++/"><![CDATA[<h1 id="what-is-a-pointer">What is a Pointer?</h1>
<p>A pointer is a variable that stores the memory address of another variable. For example - in the program below, <code class="language-plaintext highlighter-rouge">a</code> is an integer variable which represents a memory address. Let’s say <code class="language-plaintext highlighter-rouge">10</code> is stored at address <code class="language-plaintext highlighter-rouge">200</code>(here memory address is represented in integer value for explaining the concept but it is usually represented in hexadecimal form) inside computer memory. Remember that an integer usually takes 4 bytes of memory. However, now the variable <code class="language-plaintext highlighter-rouge">a</code> represents address <code class="language-plaintext highlighter-rouge">200</code>. A pointer variable is a variable which can store that address instead of storing the value <code class="language-plaintext highlighter-rouge">10</code>. However, the type of the pointer variable must be same as the data type of the variable.</p>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span><span class="cpf">&lt;iostream&gt;</span><span class="cp">
</span>
<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">a</span> <span class="o">=</span> <span class="mi">10</span><span class="p">;</span>
    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="declaring-pointers-in-c">Declaring Pointers in C++</h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>data_type* pointer_name;

// Pointer to an integer
int* intPtr;
</code></pre></div></div>
<p>Or,</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>data_type *pointer_name;

// Pointer to a character
char *cPtr;
</code></pre></div></div>

<h2 id="initializing-pointers-in-c">Initializing Pointers in C++</h2>
<p>There are two ways of initializing a pointer safely.</p>

<ol>
  <li>
    <p><strong>Assigning an existing variable’s address:</strong> The most common way of initializing a pointer is by storing the address of an already declared variable. It stores the address of the variable and can access or modify the underlying value as shown below:</p>

    <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="cp">#include</span><span class="cpf">&lt;iostream&gt;</span><span class="cp">
</span>
 <span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
     <span class="kt">int</span> <span class="n">num</span> <span class="o">=</span> <span class="mi">12</span><span class="p">;</span>
     <span class="kt">int</span><span class="o">*</span> <span class="n">ptr</span> <span class="o">=</span> <span class="o">&amp;</span><span class="n">num</span><span class="p">;</span>

     <span class="c1">// Display the stored address</span>
     <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="n">ptr</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>

     <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
 <span class="p">}</span>
</code></pre></div>    </div>
    <blockquote>
      <p>To get the address of variable <code class="language-plaintext highlighter-rouge">num</code>, you need to use the <code class="language-plaintext highlighter-rouge">&amp;</code> (also known as reference operator) operator.</p>
    </blockquote>
  </li>
  <li>
    <p><strong>Using <code class="language-plaintext highlighter-rouge">nullptr</code> (Modern C++):</strong> Sometimes we need to declare a pointer in advance, even if we don’t yet have a variable for it to point to—for example, when a pointer will later be assigned dynamically allocated memory or a value returned by a function. In such cases, we should initialize the pointer with nullptr. This prevents it from pointing to random memory and makes it safer to check before using it:</p>

    <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="c1">// Does not point anywhere</span>
 <span class="kt">int</span><span class="o">*</span> <span class="n">ptr</span> <span class="o">=</span> <span class="nb">nullptr</span><span class="p">;</span>
</code></pre></div>    </div>
  </li>
</ol>

<h2 id="dereferencing-a-pointer">Dereferencing a Pointer</h2>
<p>Dereferencing a pointer means to access or modify the data stored at the memory address held by a pointer. <code class="language-plaintext highlighter-rouge">*</code> operator is used to dereference a pointer.</p>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span><span class="cpf">&lt;iostream&gt;</span><span class="cp">
</span>
<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">num</span> <span class="o">=</span> <span class="mi">10</span><span class="p">;</span>
    <span class="kt">int</span><span class="o">*</span> <span class="n">ptr</span> <span class="o">=</span> <span class="o">&amp;</span><span class="n">num</span><span class="p">;</span>

    <span class="c1">// Display the address</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="n">ptr</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>

    <span class="c1">// Dereferencing pointer</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="o">*</span><span class="n">ptr</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>

    <span class="c1">// Modifying underlying value</span>
    <span class="o">*</span><span class="n">ptr</span> <span class="o">=</span> <span class="mi">12</span><span class="p">;</span>

    <span class="c1">// Print updated value</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="n">num</span> <span class="o">&lt;&lt;</span> <span class="o">*</span><span class="n">ptr</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>

    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>
<p>We can also change the underlying value in that address using the pointer and when we do that, both the <code class="language-plaintext highlighter-rouge">num</code> variable and the pointer variable will show the modified value.</p>

<blockquote>
  <p><strong>NOTE:</strong> If we attempt to dereference a null pointer, the program will typically crash or throw a runtime error instead of silently reading or writing garbage memory. This makes bugs easier to detect. By initializing a pointer with <code class="language-plaintext highlighter-rouge">nullptr</code>, we ensure it doesn’t accidentally access random memory, and we can safely check whether it points to a valid location before using it:</p>
</blockquote>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="p">(</span><span class="n">ptr</span> <span class="o">!=</span> <span class="nb">nullptr</span><span class="p">)</span> <span class="p">{</span> 
    <span class="o">*</span><span class="n">ptr</span> <span class="o">=</span> <span class="mi">10</span><span class="p">;</span>  <span class="c1">// Change the stored value</span>
<span class="p">}</span> 
</code></pre></div></div>

<h2 id="types-of-pointer-in-c">Types of Pointer in C++:</h2>

<h3 id="null-pointer">Null Pointer</h3>
<p>A null pointer doesn’t point to any memory location. In modern C++, it is represented using the keyword <code class="language-plaintext highlighter-rouge">nullptr</code>. Using <code class="language-plaintext highlighter-rouge">nullptr</code> helps ensure that a pointer does not accidentally reference garbage memory.</p>

<p>For example, the code block below shows how <code class="language-plaintext highlighter-rouge">nullptr</code> can prevent undefined behavior by allowing us to check whether a pointer is pointing to a valid memory location before using it:</p>

<pre><code class="language-C++">#include&lt;iostream&gt;

int main() {
    int* ptr = nullptr; 

    if (ptr != nullptr) { 
        *ptr = 10;  // Safe: This block runs only if ptr points to valid memory 
    } else { 
        std::cout &lt;&lt; "Pointer is null, cannot assign value.\n"; 
    } 

    return 0;
}
</code></pre>

<h3 id="void-pointer">Void Pointer</h3>
<p>A void pointer (also called a generic pointer) can hold the memory address of any data type, but it cannot be dereferenced directly without typecasting.</p>

<p>When the type is known at compile time and no runtime checks are needed, static casting is used:</p>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span><span class="cpf">&lt;iostream&gt;</span><span class="cp">
</span>
<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="kt">void</span><span class="o">*</span> <span class="n">ptr</span><span class="p">;</span>
    <span class="kt">int</span> <span class="n">num</span> <span class="o">=</span> <span class="mi">12</span><span class="p">;</span>
    <span class="n">ptr</span> <span class="o">=</span> <span class="o">&amp;</span><span class="n">num</span><span class="p">;</span>

    <span class="c1">// Casting to access the value</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="o">*</span><span class="p">((</span><span class="kt">int</span><span class="o">*</span><span class="p">)</span><span class="n">ptr</span><span class="p">)</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">:</span><span class="n">endl</span><span class="p">;</span>

    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<blockquote>
  <p>Casting was done by <code class="language-plaintext highlighter-rouge">(int*)</code> and to dereference the integer pointer we used <code class="language-plaintext highlighter-rouge">*((int*)ptr)</code>.</p>
</blockquote>

<h3 id="dangling-pointer">Dangling Pointer</h3>
<p>A dangling pointer is one that points to memory that has already been freed or deleted. Accessing such memory is unsafe and may crash the program.</p>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span><span class="cpf">&lt;iostream&gt;</span><span class="cp">
</span>
<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="c1">// Using heap memory to store 5</span>
    <span class="kt">int</span><span class="o">*</span> <span class="n">ptr</span> <span class="o">=</span> <span class="k">new</span> <span class="kt">int</span><span class="p">(</span><span class="mi">5</span><span class="p">);</span>

    <span class="c1">// Accessing underlying value</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="o">*</span><span class="n">ptr</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>

    <span class="c1">// Delete the pointer</span>
    <span class="k">delete</span> <span class="n">ptr</span><span class="p">;</span>
    <span class="c1">// ptr is now dangling (Invalid memory reference) </span>
    
    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="wild-pointer">Wild Pointer</h3>
<p>A wild pointer is an uninitialized pointer that points to a random memory location. Dereferencing such a pointer is dangerous because it can cause undefined behavior, crashes, or data corruption:</p>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="o">*</span><span class="n">ptr</span><span class="p">;</span>  <span class="c1">// Not initialized</span>
<span class="o">*</span><span class="n">ptr</span> <span class="o">=</span> <span class="mi">10</span><span class="p">;</span>  <span class="c1">// Crash the program with error</span>
</code></pre></div></div>

<h3 id="constant-pointer">Constant Pointer</h3>
<p>A constant pointer is a pointer that, once initialized, cannot point to a different address, but the value stored at that address can still be modified:</p>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span><span class="cpf">&lt;iostream&gt;</span><span class="cp">
</span>
<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">x</span> <span class="o">=</span> <span class="mi">5</span><span class="p">,</span> <span class="n">y</span> <span class="o">=</span> <span class="mi">10</span><span class="p">;</span>
    <span class="kt">int</span><span class="o">*</span> <span class="k">const</span> <span class="n">ptr</span> <span class="o">=</span> <span class="o">&amp;</span><span class="n">x</span><span class="p">;</span>

    <span class="c1">// Stored value can be changed</span>
    <span class="o">*</span><span class="n">ptr</span> <span class="o">=</span> <span class="mi">20</span><span class="p">;</span>

    <span class="c1">// Stored address can't be changed</span>
    <span class="n">ptr</span> <span class="o">=</span> <span class="o">&amp;</span><span class="n">y</span><span class="p">;</span>    <span class="c1">// Not allowed</span>

    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>
<blockquote>
  <p>We can also make the value constant by using <code class="language-plaintext highlighter-rouge">const</code> before the data type e.g., <code class="language-plaintext highlighter-rouge">const int* const ptr = &amp;x;</code>.</p>
</blockquote>

<h2 id="pointers-and-arrays">Pointers and Arrays</h2>
<p>In C++, when we assign an array to a pointer, the pointer refers to the first element of the array. From there, we can use pointer arithmetic (see the next section) to access different elements, just as we would with indexing.</p>

<p>When we write:</p>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="n">arr</span><span class="p">[</span><span class="mi">3</span><span class="p">]</span> <span class="o">=</span> <span class="p">{</span><span class="mi">10</span><span class="p">,</span> <span class="mi">20</span><span class="p">,</span> <span class="mi">30</span><span class="p">};</span> 
<span class="kt">int</span><span class="o">*</span> <span class="n">ptr</span> <span class="o">=</span> <span class="n">arr</span><span class="p">;</span> 
</code></pre></div></div>
<p>Here:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">arr</code> gives the address of the first element (<code class="language-plaintext highlighter-rouge">&amp;arr[0]</code>).</li>
  <li><code class="language-plaintext highlighter-rouge">ptr</code> now points to the address of the first element.</li>
</ul>

<p>Now <code class="language-plaintext highlighter-rouge">ptr</code> is equivalent to <code class="language-plaintext highlighter-rouge">arr[0]</code>, and (<code class="language-plaintext highlighter-rouge">ptr + i</code>) is equivalent to <code class="language-plaintext highlighter-rouge">arr[i]</code>.</p>

<p>Using pointers with arrays is useful because it allows more efficient traversal and manipulation of array elements, especially in functions, without copying the entire array. Pointer arithmetic can also simplify operations like iterating through dynamically allocated arrays or working with segments of an array.</p>

<p>The example below shows how we can access array elements using pointers:</p>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="c1"> </span><span class="cp">
</span><span class="k">using</span> <span class="k">namespace</span> <span class="n">std</span><span class="p">;</span> 

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span> 
    <span class="kt">double</span> <span class="n">numbers</span><span class="p">[]</span> <span class="o">=</span> <span class="p">{</span><span class="mf">2.5</span><span class="p">,</span> <span class="mf">4.5</span><span class="p">,</span> <span class="mf">6.5</span><span class="p">,</span> <span class="mf">8.5</span><span class="p">};</span> 
    <span class="kt">double</span><span class="o">*</span> <span class="n">ptr</span> <span class="o">=</span> <span class="n">numbers</span><span class="p">;</span> 

    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="mi">4</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span> 
        <span class="c1">// Using pointer arithmetic instead of array indexing</span>
        <span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"numbers["</span> <span class="o">&lt;&lt;</span> <span class="n">i</span> <span class="o">&lt;&lt;</span> <span class="s">"] = "</span> <span class="o">&lt;&lt;</span> <span class="o">*</span><span class="p">(</span><span class="n">ptr</span> <span class="o">+</span> <span class="n">i</span><span class="p">)</span> <span class="o">&lt;&lt;</span> <span class="n">endl</span><span class="p">;</span> 
    <span class="p">}</span> 

    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span> 
<span class="p">}</span>  
</code></pre></div></div>

<p>The output will be:</p>
<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">numbers</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">=</span> <span class="mf">2.5</span> 
<span class="n">numbers</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span> <span class="o">=</span> <span class="mf">4.5</span> 
<span class="n">numbers</span><span class="p">[</span><span class="mi">2</span><span class="p">]</span> <span class="o">=</span> <span class="mf">6.5</span> 
<span class="n">numbers</span><span class="p">[</span><span class="mi">3</span><span class="p">]</span> <span class="o">=</span> <span class="mf">8.5</span> 
</code></pre></div></div>

<p>Since pointers give direct access to memory, we can also change array values using them. This can be useful when we want to efficiently modify elements in an array without using the array index notation, especially in scenarios like passing arrays to functions, iterating through large datasets, or implementing dynamic data structures where direct memory manipulation improves performance:</p>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="c1"> </span><span class="cp">
</span><span class="k">using</span> <span class="k">namespace</span> <span class="n">std</span><span class="p">;</span> 

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span> 
    <span class="kt">int</span> <span class="n">arr</span><span class="p">[</span><span class="mi">3</span><span class="p">]</span> <span class="o">=</span> <span class="p">{</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">};</span>  
    <span class="kt">int</span><span class="o">*</span> <span class="n">ptr</span> <span class="o">=</span> <span class="n">arr</span><span class="p">;</span>  

    <span class="o">*</span><span class="n">ptr</span> <span class="o">=</span> <span class="mi">10</span><span class="p">;</span>             <span class="c1">// Changes arr[0] to 10  </span>
    <span class="o">*</span><span class="p">(</span><span class="n">ptr</span> <span class="o">+</span> <span class="mi">1</span><span class="p">)</span> <span class="o">=</span> <span class="mi">20</span><span class="p">;</span>  <span class="c1">// Changes arr[1] to 20  </span>

    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="mi">3</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span> 
        <span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="n">arr</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">&lt;&lt;</span> <span class="s">" "</span><span class="p">;</span> 
    <span class="p">}</span> 

    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span> 
<span class="p">}</span>  
</code></pre></div></div>

<p>The output will be:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>10 20 3 
</code></pre></div></div>

<h2 id="pointer-arithmetic">Pointer Arithmetic</h2>
<p>Pointers in C++ allows us to perform arithmetic operations using them. It allows us to move through memory locations efficiently, especially when working with arrays. However, it supports only a limited number of well-defined operations.</p>

<h3 id="increment-">Increment (++)</h3>
<p>The increment operator (++) moves the pointer to the next memory location based on its data type:</p>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="c1"> </span><span class="cp">
</span><span class="k">using</span> <span class="k">namespace</span> <span class="n">std</span><span class="p">;</span> 

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span> 
    <span class="kt">int</span> <span class="n">arr</span><span class="p">[]</span> <span class="o">=</span> <span class="p">{</span><span class="mi">10</span><span class="p">,</span> <span class="mi">20</span><span class="p">,</span> <span class="mi">30</span><span class="p">};</span>  
    <span class="kt">int</span><span class="o">*</span> <span class="n">ptr</span> <span class="o">=</span> <span class="n">arr</span><span class="p">;</span>   <span class="c1">// Points to arr[0]  </span>
    <span class="n">ptr</span><span class="o">++</span><span class="p">;</span>                <span class="c1">// Now points to arr[1]  </span>
    <span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="o">*</span><span class="n">ptr</span><span class="p">;</span> 
    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span> 
<span class="p">}</span> 
</code></pre></div></div>
<p>The output will be:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>20
</code></pre></div></div>
<p>Here, <code class="language-plaintext highlighter-rouge">ptr++</code> increases the pointer by the size of the data type (<code class="language-plaintext highlighter-rouge">int</code>) (i.e., <code class="language-plaintext highlighter-rouge">4 bytes</code>), not just by one byte. Consider the example below to understand it better:</p>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span><span class="cpf">&lt;iostream&gt;</span><span class="cp">
</span>
<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">a</span> <span class="o">=</span> <span class="mi">10</span><span class="p">;</span>
    <span class="kt">int</span><span class="o">*</span> <span class="n">ptrVar</span> <span class="o">=</span> <span class="o">&amp;</span><span class="n">a</span><span class="p">;</span>

    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="n">ptrVar</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="p">(</span><span class="n">ptrVar</span><span class="o">+</span><span class="mi">1</span><span class="p">)</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>

    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>
<p>Here, in the first statement, it’ll print a memory address and in the second statement, it’ll add <code class="language-plaintext highlighter-rouge">4</code> bytes to the initial memory address. This is because the type of the pointer variable is integer and integer takes <code class="language-plaintext highlighter-rouge">4</code> bytes of memory. Therefore, the next memory address will be at <code class="language-plaintext highlighter-rouge">(previous memory address + 4 bytes)</code>. <strong><em>We’ll get a random value if we try to dereference <code class="language-plaintext highlighter-rouge">*(p+1)</code> because we don’t know what happened with that memory address. Maybe some other programs stored that value at some points in that memory address.</em></strong></p>

<blockquote>
  <p>You can check the size of a data type using <code class="language-plaintext highlighter-rouge">sizeof</code> operator. For example - <code class="language-plaintext highlighter-rouge">sizeof(int)</code>.</p>
</blockquote>

<h3 id="decrement-">Decrement (–)</h3>
<p>The decrement operator (<code class="language-plaintext highlighter-rouge">--</code>) moves the pointer to the previous memory location:</p>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="c1"> </span><span class="cp">
</span><span class="k">using</span> <span class="k">namespace</span> <span class="n">std</span><span class="p">;</span> 

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span> 
    <span class="kt">int</span> <span class="n">arr</span><span class="p">[]</span> <span class="o">=</span> <span class="p">{</span><span class="mi">10</span><span class="p">,</span> <span class="mi">20</span><span class="p">,</span> <span class="mi">30</span><span class="p">};</span>  
    <span class="kt">int</span><span class="o">*</span> <span class="n">ptr</span> <span class="o">=</span> <span class="o">&amp;</span><span class="n">arr</span><span class="p">[</span><span class="mi">2</span><span class="p">];</span> <span class="c1">// Points to arr[2]  </span>
    <span class="n">ptr</span><span class="o">--</span><span class="p">;</span>                         <span class="c1">// Now points to arr[1]  </span>
    <span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="o">*</span><span class="n">ptr</span><span class="p">;</span> 
    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span> 
<span class="p">}</span>
</code></pre></div></div>
<p>The output will be:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>20
</code></pre></div></div>

<h3 id="addition">Addition</h3>
<p>The addition operator (<code class="language-plaintext highlighter-rouge">+</code>) can be used with pointers to move them forward in memory. When we add an integer <code class="language-plaintext highlighter-rouge">n</code> to a pointer, the pointer advances by <code class="language-plaintext highlighter-rouge">n</code> elements, not raw bytes. The actual number of bytes skipped depends on the size of the type the pointer refers to:</p>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="c1"> </span><span class="cp">
</span><span class="k">using</span> <span class="k">namespace</span> <span class="n">std</span><span class="p">;</span> 

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span> 
    <span class="kt">int</span> <span class="n">arr</span><span class="p">[]</span> <span class="o">=</span> <span class="p">{</span><span class="mi">10</span><span class="p">,</span> <span class="mi">20</span><span class="p">,</span> <span class="mi">30</span><span class="p">,</span> <span class="mi">40</span><span class="p">};</span>  
    <span class="kt">int</span><span class="o">*</span> <span class="n">ptr</span> <span class="o">=</span> <span class="n">arr</span><span class="p">;</span>  
    <span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="o">*</span><span class="p">(</span><span class="n">ptr</span> <span class="o">+</span> <span class="mi">2</span><span class="p">);</span> 
    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span> 
<span class="p">}</span> 
</code></pre></div></div>
<p>The output will be:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>30
</code></pre></div></div>
<blockquote>
  <p><strong>NOTE:</strong> If we increment a pointer beyond the valid range of the array (or object) it points to, we invoke undefined behavior. This can lead to memory corruption, program crashes, or unpredictable results. Always ensure your pointer arithmetic stays within the allocated bounds.</p>
</blockquote>

<h3 id="subtraction">Subtraction</h3>
<p>The subtraction operator (<code class="language-plaintext highlighter-rouge">-</code>) can be used with pointers to move them backward in memory. When we subtract an integer <code class="language-plaintext highlighter-rouge">n</code> from a pointer, the pointer moves back by <code class="language-plaintext highlighter-rouge">n</code> elements:</p>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="c1"> </span><span class="cp">
</span><span class="k">using</span> <span class="k">namespace</span> <span class="n">std</span><span class="p">;</span> 

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span> 
    <span class="kt">int</span> <span class="n">arr</span><span class="p">[]</span> <span class="o">=</span> <span class="p">{</span><span class="mi">10</span><span class="p">,</span> <span class="mi">20</span><span class="p">,</span> <span class="mi">30</span><span class="p">,</span> <span class="mi">40</span><span class="p">};</span>  
    <span class="kt">int</span><span class="o">*</span> <span class="n">ptr</span> <span class="o">=</span> <span class="o">&amp;</span><span class="n">arr</span><span class="p">[</span><span class="mi">3</span><span class="p">];</span>        <span class="c1">// Points to arr[3] </span>
    <span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="o">*</span><span class="p">(</span><span class="n">ptr</span> <span class="o">-</span> <span class="mi">2</span><span class="p">);</span>       <span class="c1">// Moves 2 elements back → arr[1] </span>
    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span> 
<span class="p">}</span> 
</code></pre></div></div>

<p>The output will be:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>20
</code></pre></div></div>
<p>Just like the addition operator, subtracting too far can move the pointer outside the valid range of the array (or object) it belongs to, invoking undefined behavior.</p>

<h3 id="pointer-difference">Pointer Difference</h3>
<p>We can subtract one pointer from another (as long as they both point to the same array). The result is the number of elements between them, not the number of bytes:</p>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="c1"> </span><span class="cp">
</span><span class="k">using</span> <span class="k">namespace</span> <span class="n">std</span><span class="p">;</span> 

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span> 
    <span class="kt">int</span> <span class="n">arr</span><span class="p">[]</span> <span class="o">=</span> <span class="p">{</span><span class="mi">10</span><span class="p">,</span> <span class="mi">20</span><span class="p">,</span> <span class="mi">30</span><span class="p">,</span> <span class="mi">40</span><span class="p">};</span>  
    <span class="kt">int</span><span class="o">*</span> <span class="n">p1</span> <span class="o">=</span> <span class="o">&amp;</span><span class="n">arr</span><span class="p">[</span><span class="mi">0</span><span class="p">];</span>  
    <span class="kt">int</span><span class="o">*</span> <span class="n">p2</span> <span class="o">=</span> <span class="o">&amp;</span><span class="n">arr</span><span class="p">[</span><span class="mi">3</span><span class="p">];</span>  
    <span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="p">(</span><span class="n">p2</span> <span class="o">-</span> <span class="n">p1</span><span class="p">);</span> 
    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span> 
<span class="p">}</span> 
</code></pre></div></div>

<p>The output will be:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>3
</code></pre></div></div>
<p>The output indicates that there are <code class="language-plaintext highlighter-rouge">3</code> elements between the pointers <code class="language-plaintext highlighter-rouge">p1</code> and <code class="language-plaintext highlighter-rouge">p2</code>.</p>

<h3 id="pointers-and-functions">Pointers and Functions</h3>
<p>One of the most powerful applications of pointers in C++ is their use with functions. Normally, when we pass variables to a function, they are passed by value, meaning the function receives a copy of the variable. Changes inside the function don’t affect the original variable.</p>

<p>Pointers solve this limitation by enabling pass-by-reference. Unlike C++’s built-in pass-by-reference (<code class="language-plaintext highlighter-rouge">&amp;</code>), pointers allow more flexibility—for example, passing <code class="language-plaintext highlighter-rouge">nullptr</code>, handling arrays dynamically, or modifying multiple variables through a single pointer. They also make it clear that a function may change the original data, improving both clarity and efficiency.</p>

<h3 id="passing-pointer-to-function">Passing Pointer to Function</h3>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="c1"> </span><span class="cp">
</span><span class="k">using</span> <span class="k">namespace</span> <span class="n">std</span><span class="p">;</span> 
 
<span class="kt">void</span> <span class="nf">updateValue</span><span class="p">(</span><span class="kt">int</span><span class="o">*</span> <span class="n">p</span><span class="p">)</span> <span class="p">{</span> 
    <span class="o">*</span><span class="n">p</span> <span class="o">=</span> <span class="o">*</span><span class="n">p</span> <span class="o">+</span> <span class="mi">10</span><span class="p">;</span> <span class="c1">// Modifies the original variable </span>
<span class="p">}</span> 
 
<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span> 
    <span class="kt">int</span> <span class="n">num</span> <span class="o">=</span> <span class="mi">5</span><span class="p">;</span> 
    <span class="n">updateValue</span><span class="p">(</span><span class="o">&amp;</span><span class="n">num</span><span class="p">);</span> <span class="c1">// Pass address of num </span>
    <span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"Updated value: "</span> <span class="o">&lt;&lt;</span> <span class="n">num</span> <span class="o">&lt;&lt;</span> <span class="n">endl</span><span class="p">;</span> 
    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span> 
<span class="p">}</span> 
</code></pre></div></div>

<p>The output will be:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Updated value: 15 
</code></pre></div></div>
<p>Here, <code class="language-plaintext highlighter-rouge">updateValue()</code> receives the address of <code class="language-plaintext highlighter-rouge">num</code>, and by dereferencing the pointer (<code class="language-plaintext highlighter-rouge">*p</code>), it changes the original variable.</p>

<h3 id="pointers-to-functions">Pointers to Functions</h3>
<p>C++ also allows pointers to point to functions themselves. This lets us store the address of a function in a pointer and call it indirectly, which is useful for callback mechanisms or passing functions as arguments:</p>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="c1"> </span><span class="cp">
</span><span class="k">using</span> <span class="k">namespace</span> <span class="n">std</span><span class="p">;</span> 
 
<span class="kt">void</span> <span class="nf">greet</span><span class="p">()</span> <span class="p">{</span> 
    <span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"Hello from a function!"</span> <span class="o">&lt;&lt;</span> <span class="n">endl</span><span class="p">;</span> 
<span class="p">}</span> 
 
<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span> 
    <span class="kt">void</span> <span class="p">(</span><span class="o">*</span><span class="n">funcPtr</span><span class="p">)()</span> <span class="o">=</span> <span class="o">&amp;</span><span class="n">greet</span><span class="p">;</span> <span class="c1">// Pointer to function </span>
    <span class="n">funcPtr</span><span class="p">();</span> <span class="c1">// Call function via pointer </span>
    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span> 
<span class="p">}</span> 
</code></pre></div></div>
<p>In this example, (<code class="language-plaintext highlighter-rouge">*funcPtr</code>) declares <code class="language-plaintext highlighter-rouge">funcPtr</code> as a pointer to a function. The parentheses are necessary as without them, C++ would interpret it as a function returning <code class="language-plaintext highlighter-rouge">void</code> instead of a pointer. The <code class="language-plaintext highlighter-rouge">()</code> after <code class="language-plaintext highlighter-rouge">(*funcPtr)</code> indicates that the function takes no parameters.</p>

<p>As a whole, the <code class="language-plaintext highlighter-rouge">void (*funcPtr)() = &amp;greet;</code> line assigns the address of the <code class="language-plaintext highlighter-rouge">greet</code> function to the <code class="language-plaintext highlighter-rouge">(*funcPtr)</code> pointer. The <code class="language-plaintext highlighter-rouge">&amp;</code> is optional, as the function name alone also represents its address.</p>

<p>The output will be:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Hello from a function! 
</code></pre></div></div>

<p>Pointer can be used to write <code class="language-plaintext highlighter-rouge">callback</code> function. You can learn more about function pointer <a href="">here</a>.</p>

<h2 id="advantages-and-disadvantages-of-pointers">Advantages and Disadvantages of Pointers</h2>
<h3 id="advantages">Advantages</h3>
<ul>
  <li>
    <p><strong>Efficient memory management:</strong> Pointers allow us to allocate and deallocate memory dynamically, which is essential for managing resources effectively.</p>
  </li>
  <li>
    <p><strong>Faster execution:</strong> By giving direct access to memory addresses, pointers reduce overhead and make programs more efficient.</p>
  </li>
  <li>
    <p><strong>Support for complex data structures:</strong> Pointers are the foundation for implementing linked lists, trees, graphs, and other advanced data structures.</p>
  </li>
  <li>
    <p><strong>Pass by reference:</strong> Pointers make it possible to modify variables directly inside functions without returning values.</p>
  </li>
</ul>

<h3 id="disadvantages">Disadvantages:</h3>
<ul>
  <li>
    <p><strong>Risk of memory leaks:</strong> If dynamically allocated memory is not properly freed, it can lead to wasted memory.</p>
  </li>
  <li>
    <p><strong>Dangling and wild pointers:</strong> Mismanagement of pointers (e.g., using freed or uninitialized pointers) can cause crashes or undefined behavior.</p>
  </li>
  <li>
    <p><strong>Increased complexity:</strong> Pointer syntax and logic can be difficult for beginners to understand and debug.</p>
  </li>
  <li>
    <p><strong>Security issues:</strong> Improper use of pointers can expose vulnerabilities like buffer overflows, leading to potential exploits.</p>
  </li>
</ul>

<h2 id="best-practices-for-using-c-pointers">Best Practices for Using C++ Pointers</h2>
<p>Applying these best practices will ensure effective usage of C++ pointers:</p>

<ul>
  <li>
    <p><strong>Always initialize pointers:</strong> Set pointers to a valid address or nullptr to avoid wild pointers.</p>
  </li>
  <li>
    <p><strong>Limit pointer arithmetic:</strong> Keep arithmetic minimal and always stay within array bounds to avoid undefined behavior.</p>
  </li>
  <li>
    <p><strong>Validate pointers before use:</strong> Check for nullptr before dereferencing to ensure safe access.</p>
  </li>
  <li>
    <p><strong>Use references when possible:</strong> For function parameters, prefer references over raw pointers if ownership transfer is not required.</p>
  </li>
</ul>

<h2 id="references">References:</h2>
<ul>
  <li>CodeAcademy and GeeksForGeeks</li>
</ul>]]></content><author><name></name></author><category term="tutorial" /><category term="C++" /><summary type="html"><![CDATA[Learn about pointer and it's application in C/C++]]></summary></entry><entry><title type="html">Pixi - Best Package Manager for Python and for Doing Reproducible Research</title><link href="https://sabbir-ahmed12.github.io/blog/2026/pixi-the-best-python-package-manager/" rel="alternate" type="text/html" title="Pixi - Best Package Manager for Python and for Doing Reproducible Research" /><published>2026-04-01T04:21:12+00:00</published><updated>2026-04-01T04:21:12+00:00</updated><id>https://sabbir-ahmed12.github.io/blog/2026/pixi-the-best-python-package-manager</id><content type="html" xml:base="https://sabbir-ahmed12.github.io/blog/2026/pixi-the-best-python-package-manager/"><![CDATA[<h1 id="pixi">Pixi</h1>
<p>Pixi is a fast, modern, and reproducible package management tool for developers of all backgrounds.</p>

<h2 id="introduction">Introduction</h2>

<p><code class="language-plaintext highlighter-rouge">pip</code> and <code class="language-plaintext highlighter-rouge">conda</code> are the default python package manager for Python. However, <code class="language-plaintext highlighter-rouge">uv</code> has also gained much popularity and became the go-to package manager for many people. The reasons are - it is fast, handles lockfiles, and manages virtual environments out of the box.</p>

<p>However, data science and AI projects rarely stay pure Python. Many key packages such as numpy, psycopg2 etc. contain C, C++ or Fortran extensions that require compiler installed on the system level. <code class="language-plaintext highlighter-rouge">uv</code> or <code class="language-plaintext highlighter-rouge">pip</code> can install the Python bindings, but not the system libraries underneath them. So, if you’ve worked on data science project or if you’ve used a high performance computing cluster (HPC), you might be familiar with the pip build failures. The reasons you get such errors are - missing system build tools, missing header libraries, missing wheel package etc. In order to compile the C/C++ libraries, windows requires Microsoft C++ build tools and Linux requires <code class="language-plaintext highlighter-rouge">build-essential</code> and <code class="language-plaintext highlighter-rouge">python3-dev</code> packages. Moreover, your system might be also missing some system level packages which you need to install using system level package manager such as <code class="language-plaintext highlighter-rouge">apt</code> or <code class="language-plaintext highlighter-rouge">homebrew</code>. Since <code class="language-plaintext highlighter-rouge">pip</code> and <code class="language-plaintext highlighter-rouge">uv</code> use Python Package Index (PyPI - main package index for Python with a much larger ecosystem than conda) as the source for installing packages, you’ll almost always face such errors if you want to setup an environment for your AI or ML project.</p>

<p><code class="language-plaintext highlighter-rouge">Conda</code> is a cross-platform, cross-language package ecosystem that allows users to install packages and manage environments. It manages conda packages from the Anaconda repository and Anaconda Cloud. <code class="language-plaintext highlighter-rouge">Conda</code> packages are binaries so you don’t need to have compilers available to install them. <code class="language-plaintext highlighter-rouge">Conda</code> can create isolated environments with differnt versions of Python whereas <code class="language-plaintext highlighter-rouge">Pip</code> depends on <code class="language-plaintext highlighter-rouge">virtualenv</code> or <code class="language-plaintext highlighter-rouge">venv</code> to create isolated environments. Moreover, <code class="language-plaintext highlighter-rouge">pip</code> installs dependencies in a recursive, serial loop. No effort is made to ensure that dependencies of all the packages are fulfilled simultaneously which can lead to broken environments. <code class="language-plaintext highlighter-rouge">Conda</code> takes care of that to help prevent broken environment. However, <code class="language-plaintext highlighter-rouge">conda</code> can take long time to solve dependencies. Also, it’s <code class="language-plaintext highlighter-rouge">environment.yml</code> file only lists packages that you explicitly install, not all the dependencies underneath. So, recreating the environment later may silently pull different versions of the dependencies and thus the code may not work. Most projects need packages from both <code class="language-plaintext highlighter-rouge">PyPI</code> and <code class="language-plaintext highlighter-rouge">conda-forge</code> but neither <code class="language-plaintext highlighter-rouge">pip</code> nor <code class="language-plaintext highlighter-rouge">conda</code> checks the other conflicting versions which can break the environment.</p>

<p><code class="language-plaintext highlighter-rouge">pixi</code> solves all the problems of <code class="language-plaintext highlighter-rouge">uv</code>, <code class="language-plaintext highlighter-rouge">pip</code> and <code class="language-plaintext highlighter-rouge">conda</code> by managing Python packages from <code class="language-plaintext highlighter-rouge">PyPI</code> and compiled system libraries from <code class="language-plaintext highlighter-rouge">conda-forge</code> in a single tool, with automatic lockfiles, fast dependency resolution and stores the environment inside the project itself.</p>

<h2 id="installation">Installation</h2>

<p>Install <code class="language-plaintext highlighter-rouge">pixi</code> on Linux or macOS using the command below. For windows or other installation method, you can follow the instruction in this <a href="https://pixi.prefix.dev/latest/#available-software">link</a>.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-fsSL</span> https://pixi.sh/install.sh | sh
</code></pre></div></div>

<h2 id="initialize-a-project">Initialize a Project</h2>

<p>You can create a new directory and initialize a <code class="language-plaintext highlighter-rouge">pixi</code> project using the <code class="language-plaintext highlighter-rouge">pixi init</code> command as given below. It creates a <code class="language-plaintext highlighter-rouge">pixi.toml</code> manifest that tracks dependencies and lives in version control with your code. It automatically detects your platform and sets <code class="language-plaintext highlighter-rouge">conda-forge</code> as the default channel.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pixi init &lt;project-name&gt;
<span class="nb">cd</span> &lt;project-name&gt;
</code></pre></div></div>
<p>If you run <code class="language-plaintext highlighter-rouge">cat pixi.toml</code>, you’ll find that the <code class="language-plaintext highlighter-rouge">dependencies</code> section is empty as we haven’t installed any package yet. If your project already has a <code class="language-plaintext highlighter-rouge">pyproject.toml</code> file, <code class="language-plaintext highlighter-rouge">pixi init</code> will add sections to it automatically. No separate manifest needed.</p>

<h2 id="adding-dependencies">Adding Dependencies</h2>
<p>You can install packages from both <code class="language-plaintext highlighter-rouge">conda-forge</code> and <code class="language-plaintext highlighter-rouge">PyPI</code> as below:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># conda-forge packages</span>
pixi add python geopandas gdal lightgbm

<span class="c"># PyPI packages</span>
pixi add <span class="nt">--pypi</span> scikit-learn
</code></pre></div></div>

<p>The packages and their dependencies will appear in separate sections like <code class="language-plaintext highlighter-rouge">[dependecies]</code> and <code class="language-plaintext highlighter-rouge">[pypi-dependencies]</code> for the <code class="language-plaintext highlighter-rouge">conda-forge</code> and <code class="language-plaintext highlighter-rouge">PyPI</code> channels respectively.</p>

<p>Each command updates the pixi.toml manifest and regenerates the lockfile automatically.</p>

<h2 id="activating-environment">Activating Environment</h2>
<p>Now that you’ve an environment and some packages installed, you need to activate the environment to use the installed packages. You can use the following command to run the file e.g. <code class="language-plaintext highlighter-rouge">train.py</code> without manually activating the environment:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pixi run python train.py
</code></pre></div></div>

<p>However, if you want to manually activate it to run a program or for interactive shell session, you can do it by using:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pixi shell
</code></pre></div></div>

<h2 id="multi-platform-support">Multi-Platform Support</h2>

<p>If you’re working on a MacOS or on a Windows system but later you may need to run the code in a Linux machine, you can add more platforms on your lockfile. <code class="language-plaintext highlighter-rouge">pixi</code> can generate lockfile entries for every target platform, including system libraries. In the example below, I’ve added Linux and Windows. If you read the <code class="language-plaintext highlighter-rouge">pixi.toml</code> file, you can see the target platforms under the <code class="language-plaintext highlighter-rouge">platforms</code> variable. For more information, read the <a href="https://pixi.prefix.dev/latest/workspace/multi_platform_configuration/">documentation</a>.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pixi workspace platform add linux-64 win-64
</code></pre></div></div>

<p>This command updates <code class="language-plaintext highlighter-rouge">pixi.toml</code> file with the new platforms and regenerates the lockfile with entries for all of the platforms.</p>

<h2 id="multiple-environments">Multiple Environments</h2>
<p><code class="language-plaintext highlighter-rouge">pixi</code> can manage dependencies for separate environments such as - dev or production by not only including Python packages but also system libraries. For example, you can add <code class="language-plaintext highlighter-rouge">pytest</code> and <code class="language-plaintext highlighter-rouge">ruff</code> for dev environment using the command below:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pixi add <span class="nt">--feature</span> dev pytest ruff
</code></pre></div></div>

<p>To use the feature, link it to a named environment:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pixi workspace environment add dev <span class="nt">--feature</span> dev
</code></pre></div></div>

<p>These two commands update pixi.toml with the new feature and environment. <code class="language-plaintext highlighter-rouge">dev = ["dev"]</code> means the dev environment includes default dependencies plus everything under <code class="language-plaintext highlighter-rouge">[feature.dev.dependencies]</code>.</p>

<p>To use the dev environment, pass <code class="language-plaintext highlighter-rouge">-e dev</code> to any <code class="language-plaintext highlighter-rouge">pixi</code> command:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Activate an interactive shell</span>
pixi shell <span class="nt">-e</span> dev      

<span class="c"># Run a single command</span>
pixi run <span class="nt">-e</span> dev pytest  
</code></pre></div></div>

<h2 id="built-in-task-runner">Built-in Task Runner</h2>
<p><code class="language-plaintext highlighter-rouge">uv</code> doesn’t have a built-in task runner, so teams typically manage project commands with <code class="language-plaintext highlighter-rouge">Makefiles</code>, Just, or shell scripts. These commands can be hard to remember. <code class="language-plaintext highlighter-rouge">pixi</code> has a built-in task runner that stores these commands alongside your dependencies, so no one has to memorize them.</p>

<p>To define a task, use pixi task add:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pixi task add preprocess <span class="s2">"python src/preprocess.py --input data/raw --output data/processed"</span>
pixi task add train <span class="s2">"python src/train.py"</span>
pixi task add <span class="nb">test</span> <span class="s2">"pytest tests/"</span>
</code></pre></div></div>

<p>This adds three tasks to pixi.toml:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">[</span>tasks]
preprocess <span class="o">=</span> <span class="s2">"python src/preprocess.py --input data/raw --output data/processed"</span>
train <span class="o">=</span> <span class="s2">"python src/train.py"</span>
<span class="nb">test</span> <span class="o">=</span> <span class="s2">"pytest tests/"</span>
</code></pre></div></div>

<p>To run any task, use pixi run followed by the task name:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pixi run train
pixi run <span class="nb">test</span>
</code></pre></div></div>

<p>Tasks run inside the project environment automatically, with no need to activate first.</p>

<h2 id="global-tool-installtion">Global Tool Installtion</h2>
<p>Both uv (uv tool install) and pixi support installing tools globally. These are tools like code formatters and linters that are useful everywhere but don’t belong in any specific project:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pixi global <span class="nb">install </span>ipython
pixi global <span class="nb">install </span>ruff
</code></pre></div></div>

<p>Once installed, they’re available from any directory:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ipython    <span class="c"># start interactive Python shell</span>
ruff check <span class="nb">.</span>  <span class="c"># lint any project</span>
</code></pre></div></div>

<h2 id="reproduce-the-environment">Reproduce the Environment</h2>

<p>To reproduce the environment, you just need to run the command below:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pixi <span class="nb">install</span>
</code></pre></div></div>

<h2 id="useful-links">Useful Links</h2>
<ul>
  <li>Pixi Homepage: https://pixi.prefix.dev/latest/</li>
  <li>GitHub: https://github.com/prefix-dev/pixi</li>
</ul>]]></content><author><name></name></author><category term="tutorial" /><category term="python" /><category term="pixi" /><summary type="html"><![CDATA[A hands-on tutorial on setting up a project environment using pixi]]></summary></entry><entry><title type="html">Building a 3D Slicer CLI Module from Scratch Using ITK and SlicerExecutionModel</title><link href="https://sabbir-ahmed12.github.io/blog/2026/building-a-simple-thresholding-filter-module-for-3D-slicer/" rel="alternate" type="text/html" title="Building a 3D Slicer CLI Module from Scratch Using ITK and SlicerExecutionModel" /><published>2026-03-11T04:16:12+00:00</published><updated>2026-03-11T04:16:12+00:00</updated><id>https://sabbir-ahmed12.github.io/blog/2026/building-a-simple-thresholding-filter-module-for-3D-slicer</id><content type="html" xml:base="https://sabbir-ahmed12.github.io/blog/2026/building-a-simple-thresholding-filter-module-for-3D-slicer/"><![CDATA[<h1 id="overview">Overview</h1>

<p>3D Slicer is a powerful open-source platform widely used for <strong>medical image analysis, visualization, and research</strong>. One of its most powerful features is the ability to extend its functionality using <strong>custom modules</strong>.</p>

<p>In this tutorial we will build a <strong>Slicer Command Line Interface (CLI) module from scratch</strong> using:</p>

<ul>
  <li><strong>ITK (Insight Toolkit)</strong> for image processing</li>
  <li><strong>SlicerExecutionModel</strong> for module integration</li>
  <li><strong>CMake</strong> for building the project</li>
</ul>

<p>By the end of this tutorial you will understand:</p>

<ul>
  <li>How Slicer CLI modules work</li>
  <li>How to configure projects using CMake</li>
  <li>What <code class="language-plaintext highlighter-rouge">GenerateCLP</code> is and why it is needed</li>
  <li>How to build a module from scratch</li>
  <li>How to load the module into Slicer</li>
</ul>

<hr />

<h1 id="table-of-contents">Table of Contents</h1>

<ol>
  <li>Introduction</li>
  <li>Understanding CLI Modules in Slicer</li>
  <li>Installing Dependencies</li>
  <li>Understanding CMake</li>
  <li>Building ITK</li>
  <li>Building SlicerExecutionModel</li>
  <li>Project Structure</li>
  <li>Understanding GenerateCLP</li>
  <li>Writing the XML Interface</li>
  <li>Writing the Thresholding Algorithm</li>
  <li>Writing the CMake Configuration</li>
  <li>Building the Project</li>
  <li>Running the Module</li>
  <li>Loading the Module in Slicer</li>
  <li>Conclusion</li>
</ol>

<hr />

<h2 id="1-introduction">1. Introduction</h2>

<p>Slicer modules can be implemented in three ways:</p>

<table>
  <thead>
    <tr>
      <th>Type</th>
      <th>Language</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Scripted modules</td>
      <td>Python</td>
      <td>Fast development</td>
    </tr>
    <tr>
      <td>Loadable modules</td>
      <td>C++</td>
      <td>High performance</td>
    </tr>
    <tr>
      <td>CLI modules</td>
      <td>C++ executable</td>
      <td>Simple integration with automatic GUI</td>
    </tr>
  </tbody>
</table>

<p>In this tutorial we focus on <strong>CLI modules</strong>, which are ideal when you want to:</p>

<ul>
  <li>Integrate an <strong>existing C++ or ITK algorithm</strong></li>
  <li>Automatically generate a <strong>GUI</strong></li>
  <li>Allow the algorithm to run <strong>both inside and outside Slicer</strong></li>
</ul>

<hr />

<h2 id="2-understanding-cli-modules-in-slicer">2. Understanding CLI Modules in Slicer</h2>

<p>A Slicer CLI module consists of three components:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>XML description
C++ algorithm
CMake build system
</code></pre></div></div>

<p>Workflow:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>XML → GenerateCLP → C++ executable → Slicer GUI
</code></pre></div></div>

<p>Steps:</p>

<ol>
  <li>XML describes the interface</li>
  <li><code class="language-plaintext highlighter-rouge">GenerateCLP</code> generates command-line parsing code</li>
  <li>C++ implements the algorithm</li>
  <li>Slicer loads the executable and automatically creates a GUI</li>
</ol>

<p>A major advantage of CLI modules is that the <strong>user interface and the algorithm are separated</strong>. You describe the interface once in XML, and SlicerExecutionModel handles the tedious command-line parsing and GUI generation for you.</p>

<hr />

<h2 id="3-installing-dependencies">3. Installing Dependencies</h2>

<p>You will need:</p>

<ul>
  <li><strong>CMake</strong></li>
  <li><strong>ITK</strong></li>
  <li><strong>SlicerExecutionModel</strong></li>
</ul>

<p>Clone the repositories:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/InsightSoftwareConsortium/ITK.git
git clone https://github.com/Slicer/SlicerExecutionModel.git
</code></pre></div></div>

<p>If you also want to test the module inside Slicer, make sure you already have a working 3D Slicer installation on your machine.</p>

<hr />

<h2 id="4-understanding-cmake">4. Understanding CMake</h2>

<p>CMake is a <strong>cross-platform build system generator</strong>. Instead of manually writing platform-specific build files, we write one or more <code class="language-plaintext highlighter-rouge">CMakeLists.txt</code> files that describe:</p>

<ul>
  <li>Source files</li>
  <li>Libraries</li>
  <li>Dependencies</li>
  <li>Include directories</li>
  <li>Build rules</li>
</ul>

<p>CMake then generates the actual native build system for your platform.</p>

<p>For example, CMake can generate:</p>

<table>
  <thead>
    <tr>
      <th>Generator</th>
      <th>Typical Use</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Unix Makefiles</td>
      <td>Standard Linux/macOS terminal builds</td>
    </tr>
    <tr>
      <td>Ninja</td>
      <td>Faster incremental builds</td>
    </tr>
    <tr>
      <td>Visual Studio</td>
      <td>Windows projects</td>
    </tr>
    <tr>
      <td>Xcode</td>
      <td>macOS IDE projects</td>
    </tr>
  </tbody>
</table>

<h2 id="typical-cmake-workflow">Typical CMake workflow</h2>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Source code → CMake configure step → Generated build system → Compiler
</code></pre></div></div>

<p>The modern and clean way to configure a project is:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmake <span class="nt">-S</span> <span class="nb">.</span> <span class="nt">-B</span> build
</code></pre></div></div>

<p>Here:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">-S .</code> means the source directory is the current directory</li>
  <li><code class="language-plaintext highlighter-rouge">-B build</code> means the build files will be generated in the <code class="language-plaintext highlighter-rouge">build</code> directory</li>
</ul>

<p>Then build with:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmake <span class="nt">--build</span> build
</code></pre></div></div>

<p>This is preferred over building directly inside the source tree because it keeps generated files separate from your source code.</p>

<h3 id="choosing-a-generator">Choosing a generator</h3>

<p>You can explicitly choose a generator if needed:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmake <span class="nt">-S</span> <span class="nb">.</span> <span class="nt">-B</span> build <span class="nt">-G</span> <span class="s2">"Unix Makefiles"</span>
</code></pre></div></div>

<p>or</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmake <span class="nt">-S</span> <span class="nb">.</span> <span class="nt">-B</span> build <span class="nt">-G</span> Ninja
</code></pre></div></div>

<p>If you use Unix Makefiles, you may also build with:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>make <span class="nt">-C</span> build <span class="nt">-j8</span>
</code></pre></div></div>

<p>If you use Ninja, you may build with:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ninja <span class="nt">-C</span> build
</code></pre></div></div>

<h2 id="using-an-interactive-cmake-interface">Using an interactive CMake interface</h2>

<p>If you prefer an interactive terminal interface, you can use:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ccmake <span class="nt">-S</span> <span class="nb">.</span> <span class="nt">-B</span> build
</code></pre></div></div>

<p>This is helpful when you want to inspect or modify CMake cache variables such as:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">ITK_DIR</code></li>
  <li><code class="language-plaintext highlighter-rouge">SlicerExecutionModel_DIR</code></li>
  <li>build options</li>
  <li>compiler settings</li>
</ul>

<p>There is also a graphical interface:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmake-gui
</code></pre></div></div>

<p>In short, <strong>CMake is necessary because it finds dependencies, configures the project, and generates the correct build files for your platform and compiler</strong>.</p>

<hr />

<h2 id="5-building-itk">5. Building ITK</h2>

<p>Create a build directory:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir </span>ITK-build
</code></pre></div></div>

<p>Configure:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmake <span class="nt">-S</span> ./ITK <span class="nt">-B</span> ./ITK-build
</code></pre></div></div>

<p>Build:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmake <span class="nt">--build</span> ./ITK-build
</code></pre></div></div>

<p>If you are using Makefiles, you can also build with multiple CPU cores:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>make <span class="nt">-C</span> ./ITK-build <span class="nt">-j8</span>
</code></pre></div></div>

<p>On Linux, you can check the number of available CPU cores with:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">nproc</span>
</code></pre></div></div>

<p>After building, remember the path to:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ITK-build
</code></pre></div></div>

<p>You will use this later when configuring your CLI module project.</p>

<hr />

<h2 id="6-building-slicerexecutionmodel">6. Building SlicerExecutionModel</h2>

<p>Create a build directory:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir </span>SEM-build
</code></pre></div></div>

<p>Configure:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmake <span class="nt">-S</span> ./SlicerExecutionModel <span class="nt">-B</span> ./SEM-build
</code></pre></div></div>

<p>Build:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmake <span class="nt">--build</span> ./SEM-build
</code></pre></div></div>

<p>If you are using Makefiles, you can also build with:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>make <span class="nt">-C</span> ./SEM-build <span class="nt">-j8</span>
</code></pre></div></div>

<p>One important output of this build is the tool:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GenerateCLP
</code></pre></div></div>

<p>This tool is central to how Slicer CLI modules work.</p>

<hr />

<h2 id="7-project-structure">7. Project Structure</h2>

<p>Our project will look like this:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>project-root
│
├── CMakeLists.txt
└── src
    ├── CMakeLists.txt
    └── SimpleThreshold
        ├── CMakeLists.txt
        ├── SimpleThreshold.cxx
        └── SimpleThreshold.xml
</code></pre></div></div>

<p>This structure keeps the project organized:</p>

<ul>
  <li>the root directory manages the whole project</li>
  <li><code class="language-plaintext highlighter-rouge">src</code> collects source modules</li>
  <li><code class="language-plaintext highlighter-rouge">src/SimpleThreshold</code> contains one CLI module</li>
</ul>

<hr />

<h2 id="8-understanding-generateclp">8. Understanding GenerateCLP</h2>

<p><code class="language-plaintext highlighter-rouge">GenerateCLP</code> is a tool provided by <strong>SlicerExecutionModel</strong>.</p>

<p>Its job is to read your module XML file and automatically generate C++ code for command-line parsing. Instead of writing your own parser for arguments such as:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">--inputVolume</code></li>
  <li><code class="language-plaintext highlighter-rouge">--outputVolume</code></li>
  <li><code class="language-plaintext highlighter-rouge">--lowThreshold</code></li>
  <li><code class="language-plaintext highlighter-rouge">--highThreshold</code></li>
</ul>

<p>you define them once in XML, and <code class="language-plaintext highlighter-rouge">GenerateCLP</code> creates the corresponding parser code for you.</p>

<p>For example, suppose your XML contains:</p>

<div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;double&gt;</span>
  <span class="nt">&lt;name&gt;</span>lowThreshold<span class="nt">&lt;/name&gt;</span>
  <span class="nt">&lt;longflag&gt;</span>lowThreshold<span class="nt">&lt;/longflag&gt;</span>
  <span class="nt">&lt;default&gt;</span>0<span class="nt">&lt;/default&gt;</span>
<span class="nt">&lt;/double&gt;</span>
</code></pre></div></div>

<p>From this, <code class="language-plaintext highlighter-rouge">GenerateCLP</code> generates support code so that your C++ program can directly use:</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">lowThreshold</span>
</code></pre></div></div>

<p>It also generates a header file named after your module. In this tutorial that file is:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SimpleThresholdCLP.h
</code></pre></div></div>

<p>Inside your <code class="language-plaintext highlighter-rouge">.cxx</code> file, you include it:</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">"SimpleThresholdCLP.h"</span><span class="cp">
</span></code></pre></div></div>

<p>and then call:</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">PARSE_ARGS</span><span class="p">;</span>
</code></pre></div></div>

<p>This macro parses the command-line arguments based on the XML description and populates the generated variables automatically.</p>

<p>So <code class="language-plaintext highlighter-rouge">GenerateCLP</code> is needed because it:</p>

<ul>
  <li>avoids manual argument parsing</li>
  <li>keeps the XML and the executable interface synchronized</li>
  <li>enables Slicer to generate the GUI automatically from the same interface description</li>
</ul>

<p>In practice, you usually do <strong>not</strong> run <code class="language-plaintext highlighter-rouge">GenerateCLP</code> manually. Instead, the CMake macro <code class="language-plaintext highlighter-rouge">SEMMacroBuildCLI(...)</code> calls it for you during the build.</p>

<hr />

<h2 id="9-writing-the-xml-interface">9. Writing the XML Interface</h2>

<p>The XML file defines:</p>

<ul>
  <li>Module category</li>
  <li>Visible module title</li>
  <li>Inputs</li>
  <li>Outputs</li>
  <li>Parameters</li>
</ul>

<p>Example:</p>

<div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">&lt;?xml version="1.0" encoding="utf-8"?&gt;</span>

<span class="nt">&lt;executable&gt;</span>

  <span class="nt">&lt;category&gt;</span>MyModules<span class="nt">&lt;/category&gt;</span>
  <span class="nt">&lt;title&gt;</span>SimpleThreshold<span class="nt">&lt;/title&gt;</span>

  <span class="nt">&lt;description&gt;</span>
    Simple binary thresholding using ITK.
  <span class="nt">&lt;/description&gt;</span>

  <span class="nt">&lt;parameters&gt;</span>
    <span class="nt">&lt;label&gt;</span>Input/Output<span class="nt">&lt;/label&gt;</span>

    <span class="nt">&lt;image&gt;</span>
      <span class="nt">&lt;name&gt;</span>inputVolume<span class="nt">&lt;/name&gt;</span>
      <span class="nt">&lt;longflag&gt;</span>inputVolume<span class="nt">&lt;/longflag&gt;</span>
      <span class="nt">&lt;label&gt;</span>Input Volume<span class="nt">&lt;/label&gt;</span>
      <span class="nt">&lt;channel&gt;</span>input<span class="nt">&lt;/channel&gt;</span>
      <span class="nt">&lt;description&gt;</span>Input image volume<span class="nt">&lt;/description&gt;</span>
    <span class="nt">&lt;/image&gt;</span>

    <span class="nt">&lt;image&gt;</span>
      <span class="nt">&lt;name&gt;</span>outputVolume<span class="nt">&lt;/name&gt;</span>
      <span class="nt">&lt;longflag&gt;</span>outputVolume<span class="nt">&lt;/longflag&gt;</span>
      <span class="nt">&lt;label&gt;</span>Output Volume<span class="nt">&lt;/label&gt;</span>
      <span class="nt">&lt;channel&gt;</span>output<span class="nt">&lt;/channel&gt;</span>
      <span class="nt">&lt;description&gt;</span>Output thresholded image volume<span class="nt">&lt;/description&gt;</span>
    <span class="nt">&lt;/image&gt;</span>
  <span class="nt">&lt;/parameters&gt;</span>

  <span class="nt">&lt;parameters&gt;</span>
    <span class="nt">&lt;label&gt;</span>Threshold Parameters<span class="nt">&lt;/label&gt;</span>

    <span class="nt">&lt;double&gt;</span>
      <span class="nt">&lt;name&gt;</span>lowThreshold<span class="nt">&lt;/name&gt;</span>
      <span class="nt">&lt;longflag&gt;</span>lowThreshold<span class="nt">&lt;/longflag&gt;</span>
      <span class="nt">&lt;label&gt;</span>Low Threshold<span class="nt">&lt;/label&gt;</span>
      <span class="nt">&lt;description&gt;</span>Lower threshold value<span class="nt">&lt;/description&gt;</span>
      <span class="nt">&lt;default&gt;</span>0<span class="nt">&lt;/default&gt;</span>
    <span class="nt">&lt;/double&gt;</span>

    <span class="nt">&lt;double&gt;</span>
      <span class="nt">&lt;name&gt;</span>highThreshold<span class="nt">&lt;/name&gt;</span>
      <span class="nt">&lt;longflag&gt;</span>highThreshold<span class="nt">&lt;/longflag&gt;</span>
      <span class="nt">&lt;label&gt;</span>High Threshold<span class="nt">&lt;/label&gt;</span>
      <span class="nt">&lt;description&gt;</span>Upper threshold value<span class="nt">&lt;/description&gt;</span>
      <span class="nt">&lt;default&gt;</span>0<span class="nt">&lt;/default&gt;</span>
    <span class="nt">&lt;/double&gt;</span>

    <span class="nt">&lt;double&gt;</span>
      <span class="nt">&lt;name&gt;</span>insideValue<span class="nt">&lt;/name&gt;</span>
      <span class="nt">&lt;longflag&gt;</span>insideValue<span class="nt">&lt;/longflag&gt;</span>
      <span class="nt">&lt;label&gt;</span>Inside Value<span class="nt">&lt;/label&gt;</span>
      <span class="nt">&lt;description&gt;</span>Value assigned to voxels inside the threshold range<span class="nt">&lt;/description&gt;</span>
      <span class="nt">&lt;default&gt;</span>1<span class="nt">&lt;/default&gt;</span>
    <span class="nt">&lt;/double&gt;</span>

    <span class="nt">&lt;double&gt;</span>
      <span class="nt">&lt;name&gt;</span>outsideValue<span class="nt">&lt;/name&gt;</span>
      <span class="nt">&lt;longflag&gt;</span>outsideValue<span class="nt">&lt;/longflag&gt;</span>
      <span class="nt">&lt;label&gt;</span>Outside Value<span class="nt">&lt;/label&gt;</span>
      <span class="nt">&lt;description&gt;</span>Value assigned to voxels outside the threshold range<span class="nt">&lt;/description&gt;</span>
      <span class="nt">&lt;default&gt;</span>0<span class="nt">&lt;/default&gt;</span>
    <span class="nt">&lt;/double&gt;</span>
  <span class="nt">&lt;/parameters&gt;</span>

<span class="nt">&lt;/executable&gt;</span>
</code></pre></div></div>

<h3 id="brief-explanation-of-the-xml">Brief explanation of the XML</h3>

<p>This XML defines the interface of the CLI module.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">&lt;category&gt;</code> controls where the module appears in Slicer</li>
  <li><code class="language-plaintext highlighter-rouge">&lt;title&gt;</code> controls the displayed module name</li>
  <li><code class="language-plaintext highlighter-rouge">&lt;parameters&gt;</code> groups related parameters together in the GUI</li>
  <li><code class="language-plaintext highlighter-rouge">&lt;image&gt;</code> defines image inputs and outputs</li>
  <li><code class="language-plaintext highlighter-rouge">&lt;double&gt;</code> defines numeric parameters</li>
  <li><code class="language-plaintext highlighter-rouge">&lt;name&gt;</code> becomes the generated variable name in C++</li>
  <li><code class="language-plaintext highlighter-rouge">&lt;longflag&gt;</code> defines the command-line flag name</li>
</ul>

<p>Because we use:</p>

<div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;category&gt;</span>MyModules<span class="nt">&lt;/category&gt;</span>
</code></pre></div></div>

<p>the module will appear under:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Modules → MyModules → SimpleThreshold
</code></pre></div></div>

<p>This same XML is used both to build the GUI in Slicer and to generate the parser used by the executable.</p>

<hr />

<h2 id="10-writing-the-thresholding-algorithm">10. Writing the Thresholding Algorithm</h2>

<p>Now we implement the actual algorithm using ITK.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SimpleThreshold.cxx
</code></pre></div></div>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">"itkImage.h"</span><span class="cp">
#include</span> <span class="cpf">"itkImageFileReader.h"</span><span class="cp">
#include</span> <span class="cpf">"itkImageFileWriter.h"</span><span class="cp">
#include</span> <span class="cpf">"itkBinaryThresholdImageFilter.h"</span><span class="cp">
</span>
<span class="cp">#include</span> <span class="cpf">&lt;cstdlib&gt;</span><span class="cp">
</span>
<span class="cp">#include</span> <span class="cpf">"SimpleThresholdCLP.h"</span><span class="cp">
</span>
<span class="kt">int</span> <span class="nf">main</span><span class="p">(</span><span class="kt">int</span> <span class="n">argc</span><span class="p">,</span> <span class="kt">char</span> <span class="o">*</span> <span class="n">argv</span><span class="p">[])</span>
<span class="p">{</span>
  <span class="n">PARSE_ARGS</span><span class="p">;</span>

  <span class="k">constexpr</span> <span class="kt">unsigned</span> <span class="kt">int</span> <span class="n">Dimension</span> <span class="o">=</span> <span class="mi">3</span><span class="p">;</span>
  <span class="k">using</span> <span class="n">PixelType</span> <span class="o">=</span> <span class="kt">short</span><span class="p">;</span>

  <span class="k">using</span> <span class="n">ImageType</span> <span class="o">=</span> <span class="n">itk</span><span class="o">::</span><span class="n">Image</span><span class="o">&lt;</span><span class="n">PixelType</span><span class="p">,</span> <span class="n">Dimension</span><span class="o">&gt;</span><span class="p">;</span>
  <span class="k">using</span> <span class="n">ReaderType</span> <span class="o">=</span> <span class="n">itk</span><span class="o">::</span><span class="n">ImageFileReader</span><span class="o">&lt;</span><span class="n">ImageType</span><span class="o">&gt;</span><span class="p">;</span>
  <span class="k">using</span> <span class="n">WriterType</span> <span class="o">=</span> <span class="n">itk</span><span class="o">::</span><span class="n">ImageFileWriter</span><span class="o">&lt;</span><span class="n">ImageType</span><span class="o">&gt;</span><span class="p">;</span>
  <span class="k">using</span> <span class="n">ThresholdFilterType</span> <span class="o">=</span>
      <span class="n">itk</span><span class="o">::</span><span class="n">BinaryThresholdImageFilter</span><span class="o">&lt;</span><span class="n">ImageType</span><span class="p">,</span> <span class="n">ImageType</span><span class="o">&gt;</span><span class="p">;</span>

  <span class="k">auto</span> <span class="n">reader</span> <span class="o">=</span> <span class="n">ReaderType</span><span class="o">::</span><span class="n">New</span><span class="p">();</span>
  <span class="n">reader</span><span class="o">-&gt;</span><span class="n">SetFileName</span><span class="p">(</span><span class="n">inputVolume</span><span class="p">);</span>

  <span class="k">auto</span> <span class="n">thresholder</span> <span class="o">=</span> <span class="n">ThresholdFilterType</span><span class="o">::</span><span class="n">New</span><span class="p">();</span>
  <span class="n">thresholder</span><span class="o">-&gt;</span><span class="n">SetInput</span><span class="p">(</span><span class="n">reader</span><span class="o">-&gt;</span><span class="n">GetOutput</span><span class="p">());</span>
  <span class="n">thresholder</span><span class="o">-&gt;</span><span class="n">SetLowerThreshold</span><span class="p">(</span><span class="k">static_cast</span><span class="o">&lt;</span><span class="n">PixelType</span><span class="o">&gt;</span><span class="p">(</span><span class="n">lowThreshold</span><span class="p">));</span>
  <span class="n">thresholder</span><span class="o">-&gt;</span><span class="n">SetUpperThreshold</span><span class="p">(</span><span class="k">static_cast</span><span class="o">&lt;</span><span class="n">PixelType</span><span class="o">&gt;</span><span class="p">(</span><span class="n">highThreshold</span><span class="p">));</span>
  <span class="n">thresholder</span><span class="o">-&gt;</span><span class="n">SetInsideValue</span><span class="p">(</span><span class="k">static_cast</span><span class="o">&lt;</span><span class="n">PixelType</span><span class="o">&gt;</span><span class="p">(</span><span class="n">insideValue</span><span class="p">));</span>
  <span class="n">thresholder</span><span class="o">-&gt;</span><span class="n">SetOutsideValue</span><span class="p">(</span><span class="k">static_cast</span><span class="o">&lt;</span><span class="n">PixelType</span><span class="o">&gt;</span><span class="p">(</span><span class="n">outsideValue</span><span class="p">));</span>

  <span class="k">auto</span> <span class="n">writer</span> <span class="o">=</span> <span class="n">WriterType</span><span class="o">::</span><span class="n">New</span><span class="p">();</span>
  <span class="n">writer</span><span class="o">-&gt;</span><span class="n">SetFileName</span><span class="p">(</span><span class="n">outputVolume</span><span class="p">);</span>
  <span class="n">writer</span><span class="o">-&gt;</span><span class="n">SetInput</span><span class="p">(</span><span class="n">thresholder</span><span class="o">-&gt;</span><span class="n">GetOutput</span><span class="p">());</span>

  <span class="n">writer</span><span class="o">-&gt;</span><span class="n">Update</span><span class="p">();</span>

  <span class="k">return</span> <span class="n">EXIT_SUCCESS</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="brief-explanation-of-the-thresholding-code">Brief explanation of the thresholding code</h3>

<p>This code performs binary thresholding on a 3D image. It sets every pixel in the range <code class="language-plaintext highlighter-rouge">[lowThreshold, highThreshold]</code> to <code class="language-plaintext highlighter-rouge">insideValue</code> and all other pixels to the <code class="language-plaintext highlighter-rouge">outsideValue</code>.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">PARSE_ARGS;</code> loads the parameters defined in the XML</li>
  <li><code class="language-plaintext highlighter-rouge">Dimension = 3</code> means we are processing a 3D volume</li>
  <li><code class="language-plaintext highlighter-rouge">PixelType = short</code> means the image stores signed-short intensities</li>
  <li><code class="language-plaintext highlighter-rouge">ImageFileReader</code> reads the input image</li>
  <li><code class="language-plaintext highlighter-rouge">BinaryThresholdImageFilter</code> performs the thresholding</li>
  <li><code class="language-plaintext highlighter-rouge">SetLowerThreshold(...)</code> and <code class="language-plaintext highlighter-rouge">SetUpperThreshold(...)</code> define the valid intensity range</li>
  <li><code class="language-plaintext highlighter-rouge">SetInsideValue(...)</code> assigns a value to voxels inside the threshold range</li>
  <li><code class="language-plaintext highlighter-rouge">SetOutsideValue(...)</code> assigns a value to voxels outside the threshold range</li>
  <li><code class="language-plaintext highlighter-rouge">ImageFileWriter</code> writes the output image</li>
</ul>

<p>Because ITK chooses the file format from the output filename extension, using:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>output.nii.gz
</code></pre></div></div>

<p>will save the result in NIfTI format.</p>

<hr />

<h2 id="11-writing-the-cmake-configuration">11. Writing the CMake Configuration</h2>

<h3 id="root-cmakeliststxt">Root <code class="language-plaintext highlighter-rouge">CMakeLists.txt</code></h3>

<div class="language-cmake highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cmake_minimum_required</span><span class="p">(</span>VERSION 3.16<span class="p">)</span>

<span class="nb">project</span><span class="p">(</span>SimpleThresholdModule LANGUAGES CXX<span class="p">)</span>

<span class="nb">find_package</span><span class="p">(</span>ITK REQUIRED<span class="p">)</span>
<span class="nb">include</span><span class="p">(</span><span class="si">${</span><span class="nv">ITK_USE_FILE</span><span class="si">}</span><span class="p">)</span>

<span class="nb">find_package</span><span class="p">(</span>SlicerExecutionModel REQUIRED GenerateCLP<span class="p">)</span>
<span class="nb">include</span><span class="p">(</span><span class="si">${</span><span class="nv">GenerateCLP_USE_FILE</span><span class="si">}</span><span class="p">)</span>
<span class="nb">include</span><span class="p">(</span><span class="si">${</span><span class="nv">SlicerExecutionModel_USE_FILE</span><span class="si">}</span><span class="p">)</span>
<span class="nb">include</span><span class="p">(</span><span class="si">${</span><span class="nv">SlicerExecutionModel_CMAKE_DIR</span><span class="si">}</span>/SEMMacroBuildCLI.cmake<span class="p">)</span>

<span class="nb">add_subdirectory</span><span class="p">(</span>src<span class="p">)</span>
</code></pre></div></div>

<h3 id="brief-explanation">Brief explanation</h3>

<p>This file configures the overall project.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">cmake_minimum_required(...)</code> sets the minimum required CMake version</li>
  <li><code class="language-plaintext highlighter-rouge">project(...)</code> names the project and declares the language</li>
  <li><code class="language-plaintext highlighter-rouge">find_package(ITK REQUIRED)</code> finds the ITK installation</li>
  <li><code class="language-plaintext highlighter-rouge">include(${ITK_USE_FILE})</code> brings in ITK compiler and include settings</li>
  <li><code class="language-plaintext highlighter-rouge">find_package(SlicerExecutionModel REQUIRED GenerateCLP)</code> finds SlicerExecutionModel and the <code class="language-plaintext highlighter-rouge">GenerateCLP</code> tool</li>
  <li>the <code class="language-plaintext highlighter-rouge">include(...)</code> lines expose the macros needed for building CLI modules</li>
  <li><code class="language-plaintext highlighter-rouge">add_subdirectory(src)</code> tells CMake to continue processing the source tree</li>
</ul>

<h3 id="srccmakeliststxt"><code class="language-plaintext highlighter-rouge">src/CMakeLists.txt</code></h3>

<div class="language-cmake highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">add_subdirectory</span><span class="p">(</span>SimpleThreshold<span class="p">)</span>
</code></pre></div></div>

<h3 id="brief-explanation-1">Brief explanation</h3>

<p>This file simply tells CMake to process the <code class="language-plaintext highlighter-rouge">SimpleThreshold</code> module directory.</p>

<h3 id="module-cmakeliststxt">Module <code class="language-plaintext highlighter-rouge">CMakeLists.txt</code></h3>

<div class="language-cmake highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">set</span><span class="p">(</span>MODULE_NAME SimpleThreshold<span class="p">)</span>

<span class="nf">SEMMacroBuildCLI</span><span class="p">(</span>
  NAME <span class="si">${</span><span class="nv">MODULE_NAME</span><span class="si">}</span>
  TARGET_LIBRARIES <span class="si">${</span><span class="nv">ITK_LIBRARIES</span><span class="si">}</span>
  EXECUTABLE_ONLY
<span class="p">)</span>
</code></pre></div></div>

<h3 id="brief-explanation-2">Brief explanation</h3>

<p>This file builds the actual CLI module.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">MODULE_NAME</code> defines the module name</li>
  <li><code class="language-plaintext highlighter-rouge">SEMMacroBuildCLI(...)</code> is a helper macro from SlicerExecutionModel</li>
  <li>it runs <code class="language-plaintext highlighter-rouge">GenerateCLP</code> on <code class="language-plaintext highlighter-rouge">SimpleThreshold.xml</code></li>
  <li>it generates <code class="language-plaintext highlighter-rouge">SimpleThresholdCLP.h</code></li>
  <li>it builds the executable</li>
  <li>it links the executable against ITK</li>
</ul>

<hr />

<h2 id="12-building-the-project">12. Building the Project</h2>

<p>First configure the project:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmake <span class="nt">-S</span> <span class="nb">.</span> <span class="nt">-B</span> build <span class="se">\</span>
  <span class="nt">-DITK_DIR</span><span class="o">=</span>/path/to/ITK-build <span class="se">\</span>
  <span class="nt">-DSlicerExecutionModel_DIR</span><span class="o">=</span>/path/to/SEM-build
</code></pre></div></div>

<p>Then build it:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmake <span class="nt">--build</span> build
</code></pre></div></div>

<p>If your generator is Unix Makefiles, you can also build with:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>make <span class="nt">-C</span> build <span class="nt">-j8</span>
</code></pre></div></div>

<p>If you want an interactive configuration interface, you can use:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ccmake <span class="nt">-S</span> <span class="nb">.</span> <span class="nt">-B</span> build
</code></pre></div></div>

<p>The executable will typically appear in:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>build/src/SimpleThreshold/bin
</code></pre></div></div>

<hr />

<h2 id="13-running-the-module">13. Running the Module</h2>

<p>Run from the command line:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SimpleThreshold <span class="se">\</span>
  <span class="nt">--inputVolume</span> input.nii.gz <span class="se">\</span>
  <span class="nt">--outputVolume</span> output.nii.gz <span class="se">\</span>
  <span class="nt">--lowThreshold</span> 100 <span class="se">\</span>
  <span class="nt">--highThreshold</span> 500 <span class="se">\</span>
  <span class="nt">--insideValue</span> 1 <span class="se">\</span>
  <span class="nt">--outsideValue</span> 0
</code></pre></div></div>

<p>This will write the thresholded image to <code class="language-plaintext highlighter-rouge">output.nii.gz</code>.</p>

<p>You can also inspect the generated command-line interface using:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>./SimpleThreshold <span class="nt">--help</span>
</code></pre></div></div>

<hr />

<h2 id="14-loading-the-module-in-slicer">14. Loading the Module in Slicer</h2>

<p>Start Slicer with:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Slicer <span class="nt">--additional-module-paths</span> /path/to/build/src/SimpleThreshold/bin
</code></pre></div></div>

<p>Your module will appear in:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Modules → MyModules → SimpleThreshold
</code></pre></div></div>

<p>The GUI is generated automatically from the XML file.</p>

<p>If Slicer cannot save additional module paths in its settings, launching it with <code class="language-plaintext highlighter-rouge">--additional-module-paths</code> is a reliable workaround during development.</p>

<hr />

<h2 id="15-conclusion">15. Conclusion</h2>

<p>In this tutorial we built a <strong>complete Slicer CLI module from scratch</strong> using:</p>

<ul>
  <li>ITK</li>
  <li>SlicerExecutionModel</li>
  <li>CMake</li>
</ul>

<p>Key takeaways:</p>

<ul>
  <li>CLI modules separate <strong>interface, algorithm, and build system</strong></li>
  <li>XML defines the user interface</li>
  <li><code class="language-plaintext highlighter-rouge">GenerateCLP</code> handles argument parsing automatically</li>
  <li>ITK implements the image processing</li>
  <li>CMake manages dependencies and builds the executable</li>
  <li>Useful links:
    <ul>
      <li><a href="https://docs.itk.org/en/latest/">ITK documentation</a></li>
      <li><a href="https://itk.org/ItkSoftwareGuide.pdf">ITK software guide</a></li>
      <li><a href="https://www.slicer.org/wiki/Documentation/Nightly/Developers/SlicerExecutionModel">Slicer Execution Model</a></li>
    </ul>
  </li>
</ul>

<p>This workflow makes it much easier to integrate custom medical image processing algorithms into 3D Slicer without manually building a GUI.</p>

<hr />

<p><em>Congratulations! You can create a Slicer module using your custom algorithm.</em></p>]]></content><author><name></name></author><category term="tutorial" /><category term="itk" /><category term="slicer" /><category term="cmake" /><category term="c++" /><summary type="html"><![CDATA[A complete tutorial on building a Slicer CLI module using ITK and SlicerExecutionModel]]></summary></entry><entry><title type="html">Python Code Formatting and Linting Using Ruff</title><link href="https://sabbir-ahmed12.github.io/blog/2026/python-code-formatting-and-linting-using-ruff/" rel="alternate" type="text/html" title="Python Code Formatting and Linting Using Ruff" /><published>2026-01-19T04:16:12+00:00</published><updated>2026-01-19T04:16:12+00:00</updated><id>https://sabbir-ahmed12.github.io/blog/2026/python-code-formatting-and-linting-using-ruff</id><content type="html" xml:base="https://sabbir-ahmed12.github.io/blog/2026/python-code-formatting-and-linting-using-ruff/"><![CDATA[<h2 id="ruff">Ruff</h2>

<p><code class="language-plaintext highlighter-rouge">ruff</code> is a super-fast Python linter and formatter written in Rust. It consolidates multiple Python tools like Flake8, Black, isort and others into a single, faster solution that significantly improves code quality. In this tutorial, we’ll learn how to use <code class="language-plaintext highlighter-rouge">ruff</code> from the command line to check and fix our code, and how to configure <code class="language-plaintext highlighter-rouge">ruff</code> both for individual projects and globally. We’ll also learn how to integrate <code class="language-plaintext highlighter-rouge">ruff</code> with VSCode to get real-time feedback as we code.</p>

<h2 id="installation">Installation</h2>

<p>Assuming we’ve the <code class="language-plaintext highlighter-rouge">uv</code> already installed in our system, we can install <code class="language-plaintext highlighter-rouge">ruff</code> using <code class="language-plaintext highlighter-rouge">uv</code> globally using the command below:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv tool <span class="nb">install </span>ruff
</code></pre></div></div>

<p>We can check if <code class="language-plaintext highlighter-rouge">ruff</code> has been installed properly or not using <code class="language-plaintext highlighter-rouge">ruff --version</code> command.</p>

<h2 id="use-ruff-to-check-linting">Use Ruff to Check Linting</h2>

<p>We can check any issues within a Python file e.g. <code class="language-plaintext highlighter-rouge">main.py</code> using <code class="language-plaintext highlighter-rouge">ruff</code> with the following command:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ruff check main.py
</code></pre></div></div>

<p>However, if we want to check all the files in a directory, we can use:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ruff check <span class="nb">.</span>
</code></pre></div></div>

<p>Ruff can automatically fix some issues. For that, we need to use the command using <code class="language-plaintext highlighter-rouge">--fix</code> flag. For example:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ruff check <span class="nt">--fix</span> <span class="nb">.</span>
</code></pre></div></div>

<p>We can also check what <code class="language-plaintext highlighter-rouge">ruff</code> will change if we use the <code class="language-plaintext highlighter-rouge">--fix</code> flag before <code class="language-plaintext highlighter-rouge">ruff</code> actually changes our code. It’s better to be aware of the changes before it breaks the code.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ruff check <span class="nt">--fix</span> <span class="nt">--diff</span> <span class="nb">.</span>
</code></pre></div></div>

<h2 id="use-ruff-to-format-code">Use Ruff to Format Code</h2>

<p>Ruff comes with <code class="language-plaintext highlighter-rouge">Black</code> compatible code formatter. We can format our code using <code class="language-plaintext highlighter-rouge">ruff</code> using the command below:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ruff format <span class="nb">.</span>
</code></pre></div></div>

<p><strong>Run the <code class="language-plaintext highlighter-rouge">ruff check</code> command before <code class="language-plaintext highlighter-rouge">run format</code></strong>. If we want <code class="language-plaintext highlighter-rouge">ruff</code> to watch our code for error continuously, we can use the following command:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ruff check <span class="nt">--watch</span> <span class="nb">.</span>
</code></pre></div></div>

<h2 id="configure-ruff">Configure Ruff</h2>

<p>Ruff comes with some default rules already setup which are non-controversial. However, we can configure <code class="language-plaintext highlighter-rouge">ruff</code> in our own way. We can configure <code class="language-plaintext highlighter-rouge">ruff</code> globally to apply the rules to any project and locally to apply rules to a particular project. If we use local configuration, it’ll overwrite the global rules.</p>

<h3 id="configure-ruff-locally">Configure Ruff Locally</h3>

<p>We can setup <code class="language-plaintext highlighter-rouge">ruff</code> using either <code class="language-plaintext highlighter-rouge">pyproject.toml</code> file or <code class="language-plaintext highlighter-rouge">ruff.toml</code> for individual project. In <code class="language-plaintext highlighter-rouge">pyproject.toml</code> file, there maybe different sections describing different things related to the project. However, we need a different section to configure <code class="language-plaintext highlighter-rouge">ruff</code> which is [tool.ruff].</p>

<p>Let’s say we want to apply all linting rules in <code class="language-plaintext highlighter-rouge">E</code> (Pycodestyle) and <code class="language-plaintext highlighter-rouge">F</code> (Pyflakes) but we want to ignore a particular linting rule such as <code class="language-plaintext highlighter-rouge">F401</code>, we can do it in the following way:</p>

<div class="language-toml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">[</span><span class="n">tool</span><span class="k">.</span><span class="n">ruff</span><span class="k">.</span><span class="n">lint</span><span class="k">]</span>
<span class="n">select</span> <span class="o">=</span><span class="w"> </span><span class="p">[</span><span class="s">"E"</span><span class="p">,</span> <span class="s">"F"</span><span class="p">]</span>
<span class="n">ignore</span> <span class="o">=</span><span class="w"> </span><span class="p">[</span><span class="s">"F401"</span><span class="p">]</span>
</code></pre></div></div>

<p>If we instead want to use <code class="language-plaintext highlighter-rouge">ruff.toml</code> file, we can write it as below:</p>

<div class="language-toml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">[</span><span class="n">lint</span><span class="k">]</span>
<span class="n">select</span> <span class="o">=</span><span class="w"> </span><span class="p">[</span><span class="s">"E"</span><span class="p">,</span> <span class="s">"F"</span><span class="p">]</span>
<span class="n">ignore</span> <span class="o">=</span><span class="w"> </span><span class="p">[</span><span class="s">"F401"</span><span class="p">]</span>
</code></pre></div></div>

<p>You can learn more about the linting and formatting rules from the link given at the end of the post.</p>

<h3 id="configure-ruff-globally">Configure Ruff Globally</h3>

<p>To use global configuration for <code class="language-plaintext highlighter-rouge">ruff</code>, we can create a directory at <code class="language-plaintext highlighter-rouge">~/.config</code> called <code class="language-plaintext highlighter-rouge">ruff</code> and create a file named <code class="language-plaintext highlighter-rouge">ruff.toml</code> in that directory. For Windows, it can be inside the <code class="language-plaintext highlighter-rouge">AppData\Roaming</code> directory. Once the rules are set in the <code class="language-plaintext highlighter-rouge">ruff.toml</code> file, we can use the same <code class="language-plaintext highlighter-rouge">ruff check</code> command to look for issues in the code using the global configuration. We can write <code class="language-plaintext highlighter-rouge">ruff.toml</code> file as given in the <a href="https://docs.astral.sh/ruff/configuration/">here</a>.</p>

<h3 id="integrate-ruff-into-vscode">Integrate Ruff into VSCode</h3>

<p>We can also integrate <code class="language-plaintext highlighter-rouge">ruff</code> in the VSCode to fix the linting and formatting issues as we code. For that, go to the <code class="language-plaintext highlighter-rouge">Extensions</code> option and search for <code class="language-plaintext highlighter-rouge">Ruff</code>. Install the one from <code class="language-plaintext highlighter-rouge">Atral Software</code>. Once installed, you can update your <code class="language-plaintext highlighter-rouge">settings.json</code> file to use <code class="language-plaintext highlighter-rouge">ruff</code>. You can get help if you read the details of extension documentation but you can do it in the following way:</p>

<ul>
  <li>Press <code class="language-plaintext highlighter-rouge">command + shift + p</code> to open the command palette.</li>
  <li>Type <code class="language-plaintext highlighter-rouge">Preferences: Open User Setting (JSON)</code>.</li>
  <li>Press <code class="language-plaintext highlighter-rouge">Enter</code> to open the <code class="language-plaintext highlighter-rouge">setting.json</code> file.</li>
  <li>Check if you’ve any rules already setup for <code class="language-plaintext highlighter-rouge">Python</code>. Otherwise, you can paste the following:</li>
</ul>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"[python]"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"editor.formatOnSave"</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="p">,</span><span class="w">
    </span><span class="nl">"editor.codeActionsOnSave"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
      </span><span class="nl">"source.fixAll"</span><span class="p">:</span><span class="w"> </span><span class="s2">"explicit"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"source.organizeImports"</span><span class="p">:</span><span class="w"> </span><span class="s2">"explicit"</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="nl">"editor.defaultFormatter"</span><span class="p">:</span><span class="w"> </span><span class="s2">"charliermarsh.ruff"</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>However, we can still use the <code class="language-plaintext highlighter-rouge">pyproject.toml</code> and <code class="language-plaintext highlighter-rouge">ruff.toml</code> files in our project to overwrite the rules of the extension.</p>

<h2 id="useful-links">Useful Links</h2>

<p>Ruff documentation - https://docs.astral.sh/ruff/</p>

<p>Ruff rules - https://docs.astral.sh/ruff/rules/</p>

<p>Video tutorial - https://youtu.be/828S-DMQog8</p>]]></content><author><name></name></author><category term="tutorial" /><category term="python" /><summary type="html"><![CDATA[Tutorial Post on Python Code formatting and Linting]]></summary></entry><entry><title type="html">Python Project Setup Using UV</title><link href="https://sabbir-ahmed12.github.io/blog/2025/python-project-setup-using-uv/" rel="alternate" type="text/html" title="Python Project Setup Using UV" /><published>2025-05-21T04:19:00+00:00</published><updated>2025-05-21T04:19:00+00:00</updated><id>https://sabbir-ahmed12.github.io/blog/2025/python-project-setup-using-uv</id><content type="html" xml:base="https://sabbir-ahmed12.github.io/blog/2025/python-project-setup-using-uv/"><![CDATA[<h2 id="uv">UV</h2>
<hr />

<p><code class="language-plaintext highlighter-rouge">uv</code> is a new and an extremely fast <code class="language-plaintext highlighter-rouge">Python</code> package and project manager, written in Rust. <code class="language-plaintext highlighter-rouge">uv</code> uses a global caching system which makes it more efficient in terms of storage and package installation time. Unlike <code class="language-plaintext highlighter-rouge">pip</code> and other package managers, if there are multiple projects that needs a package (for example - <code class="language-plaintext highlighter-rouge">flask</code>) at a time, <code class="language-plaintext highlighter-rouge">uv</code> will install it once and use the same package for all the current projects without any issue which saves storage space by avoiding duplicate installation and also saves time.</p>

<h2 id="installation">Installation</h2>
<hr />

<p>There are different ways to install UV. You can find different ways of installing UV from <a href="https://docs.astral.sh/uv/#installation">here</a>. However, in this tutorial, we’ll use <code class="language-plaintext highlighter-rouge">Homebrew</code> to install UV as it is easier to install packages using <code class="language-plaintext highlighter-rouge">Homebrew</code> in MacOS and Linux. You can install <code class="language-plaintext highlighter-rouge">Homebrew</code> from <a href="https://brew.sh/">here</a>. You can install <code class="language-plaintext highlighter-rouge">uv</code> using <code class="language-plaintext highlighter-rouge">Homebrew</code> using the following command:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew <span class="nb">install </span>uv
</code></pre></div></div>

<p>Once it is installed successfully, you can run the following command to check if that works fine.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv <span class="nt">--version</span>
</code></pre></div></div>

<p>It should print the version of <code class="language-plaintext highlighter-rouge">uv</code>.</p>

<h2 id="setup-project-using-uv">Setup Project Using UV</h2>
<hr />

<p>Let’s say you want to create a project named <code class="language-plaintext highlighter-rouge">new_project</code> in your Desktop. For that, change your current directory to <code class="language-plaintext highlighter-rouge">Desktop</code> and write the following command:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv init new_project
</code></pre></div></div>

<p>If the command works fine, it’ll show a message with the location where it initialized that project. This command will create a new directory in the Desktop called <code class="language-plaintext highlighter-rouge">new_project</code>. However, if you want to use an existing directory to setup your application, you can run the command below:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv init
</code></pre></div></div>

<p>We aren’t using any options in the above commands. However, <code class="language-plaintext highlighter-rouge">uv</code> has some options to choose from to create project. <code class="language-plaintext highlighter-rouge">--app</code> is the default option when we run <code class="language-plaintext highlighter-rouge">uv init &lt;project_name&gt;</code>. That means, it instead runs the command <code class="language-plaintext highlighter-rouge">uv init &lt;project_name&gt; --app</code> by default. Most of the time we want to use the default option as it creates a simple project structure. We can also use another option <code class="language-plaintext highlighter-rouge">--lib</code> if we want to work on a project that will be built and distributed as a python package.</p>

<p>The default option will create some files initially in the desired directory. It’ll initialize the directory as a <code class="language-plaintext highlighter-rouge">git</code> repository. So, there will be a <code class="language-plaintext highlighter-rouge">.git</code> directory in there. The other files are:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">.gitignore</code>: contains the files and directories to ignore in the commit.</li>
  <li><code class="language-plaintext highlighter-rouge">.python-version</code>: file containing information about the version of python the app is using.</li>
  <li><code class="language-plaintext highlighter-rouge">main.py</code>: the main python file of the project.</li>
  <li><code class="language-plaintext highlighter-rouge">pyproject.toml</code>: contains information about the project e.g., name, version, description, dependencies etc.</li>
  <li><code class="language-plaintext highlighter-rouge">README.md</code>: to add documentation for the project.</li>
</ul>

<h2 id="install-and-remove-python-package">Install and Remove Python Package</h2>
<hr />

<p>Instead of using <code class="language-plaintext highlighter-rouge">pip</code>, we’ll install required package using the following command in <code class="language-plaintext highlighter-rouge">uv</code>:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv add &lt;package_name&gt;
</code></pre></div></div>

<p>For example, if you want to install <code class="language-plaintext highlighter-rouge">flask</code> and <code class="language-plaintext highlighter-rouge">requests</code> library using <code class="language-plaintext highlighter-rouge">uv</code>, you can run the following command:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv add flask requests
</code></pre></div></div>

<p>It’ll automatically create a virtual environment for the project and show the path of the environment. It’ll create a directory called <code class="language-plaintext highlighter-rouge">.venv</code> in the root directory and update the <code class="language-plaintext highlighter-rouge">pyproject.toml</code> file to list the dependencies. Moreover, it’ll create a new file called <code class="language-plaintext highlighter-rouge">uv.lock</code> which records the exact version of all the dependencies of the project. It ensures that our environment is perfectly reproducible. You can run the following command from your project directory to visualize and understand the dependencies of your project:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv tree
</code></pre></div></div>

<p>Now, to remove a <code class="language-plaintext highlighter-rouge">python</code> package from your environment, use the following command:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv remove &lt;package_name&gt;
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">uv</code> will automatically update the <code class="language-plaintext highlighter-rouge">uv.lock</code> and <code class="language-plaintext highlighter-rouge">pyproject.toml</code> file when you add and remove a package.</p>

<h2 id="run-python-script-in-uv-environment">Run Python Script in UV Environment</h2>
<hr />

<p>Now we have a <code class="language-plaintext highlighter-rouge">python</code> environment setup for our project. In old system, to run a <code class="language-plaintext highlighter-rouge">python</code> script, we used to use the command <code class="language-plaintext highlighter-rouge">python &lt;script.py&gt;</code>. However, as you’ve noticed that we don’t activate any environment in <code class="language-plaintext highlighter-rouge">uv</code>. Therefore, in order to run a script in <code class="language-plaintext highlighter-rouge">uv</code> we use the command:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv run &lt;script_name.py&gt;
</code></pre></div></div>

<p>For example - to run <code class="language-plaintext highlighter-rouge">main.py</code> file in your project, you’ll write <code class="language-plaintext highlighter-rouge">uv run main.py</code>. Now, to make sure that <code class="language-plaintext highlighter-rouge">uv</code> is using the python environment created for that particular project, you can write the following code in your <code class="language-plaintext highlighter-rouge">main.py</code> file and then run the <code class="language-plaintext highlighter-rouge">uv run main.py</code> command:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">sys</span>


<span class="k">def</span> <span class="nf">main</span><span class="p">():</span>
    <span class="nf">print</span><span class="p">(</span><span class="n">sys</span><span class="p">.</span><span class="n">executable</span><span class="p">)</span>


<span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="sh">"</span><span class="s">__main__</span><span class="sh">"</span><span class="p">:</span>
    <span class="nf">main</span><span class="p">()</span>
</code></pre></div></div>

<p>If you run your <code class="language-plaintext highlighter-rouge">main.py</code> file now, you’ll see that the python executable path is <code class="language-plaintext highlighter-rouge">&lt;path&gt;/&lt;your_app_name&gt;/.venv/bin/python3</code> which means that <code class="language-plaintext highlighter-rouge">uv</code> is using the correct <code class="language-plaintext highlighter-rouge">python</code>.</p>

<p><strong>NOTE:</strong> <strong><em>The good thing about <code class="language-plaintext highlighter-rouge">uv</code> is that even if you delete the <code class="language-plaintext highlighter-rouge">.venv/</code> directory from the project directory and then you run the <code class="language-plaintext highlighter-rouge">uv run main.py</code> command, <code class="language-plaintext highlighter-rouge">uv</code> will automatically create the environment and install the required packages using the <code class="language-plaintext highlighter-rouge">uv.lock</code> file and run the command.</em></strong></p>

<h2 id="replicate-uv-python-environment">Replicate UV Python Environment</h2>
<hr />

<p>Let’s say you want to share your code with others. Whoever wants to run your code, that person needs to create the same environment in order to run the code successfully in the new machine. However, you don’t ship your virtual environment with your code because it’ll increase the size of the repository.</p>

<p><code class="language-plaintext highlighter-rouge">uv</code> can create the exact environment using the <code class="language-plaintext highlighter-rouge">uv.lock</code> and <code class="language-plaintext highlighter-rouge">pyproject.toml</code> file shipped along with the code. To do that, use the following command:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv <span class="nb">sync</span>
</code></pre></div></div>

<h2 id="migrate-from-pip-to-uv">Migrate from <code class="language-plaintext highlighter-rouge">pip</code> to <code class="language-plaintext highlighter-rouge">uv</code></h2>
<hr />

<p>If you’ve an existing project created either using <code class="language-plaintext highlighter-rouge">pip</code> or other package managers with a <code class="language-plaintext highlighter-rouge">requirements.txt</code> file and you want to migrate that project to <code class="language-plaintext highlighter-rouge">uv</code> environment, you can migrate using the following commands:</p>

<ol>
  <li>Create the <code class="language-plaintext highlighter-rouge">uv</code> environment project structure.</li>
</ol>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv init
</code></pre></div></div>

<ol>
  <li>Install the packages</li>
</ol>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv add <span class="nt">-r</span> requirements.txt
</code></pre></div></div>

<p>Now, if you want to rely solely on <code class="language-plaintext highlighter-rouge">uv</code> in future, you can delete the <code class="language-plaintext highlighter-rouge">requirements.txt</code> file.</p>

<h2 id="replace-pipx-with-uv">Replace <code class="language-plaintext highlighter-rouge">pipx</code> with <code class="language-plaintext highlighter-rouge">uv</code></h2>
<hr />

<p><code class="language-plaintext highlighter-rouge">pipx</code> is used to install tools such as formatter, linter etc in to format and write python code following best practices since we don’t want to install those tools for a particular project separately. So, <code class="language-plaintext highlighter-rouge">pipx</code> installs those tools globally which we can now do using <code class="language-plaintext highlighter-rouge">uv</code>.</p>

<p>For example - if you want to install <code class="language-plaintext highlighter-rouge">ruff</code> using <code class="language-plaintext highlighter-rouge">uv</code>, you run the following command:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv tool <span class="nb">install </span>ruff
</code></pre></div></div>
<p>This command installs ruff and will add to the <code class="language-plaintext highlighter-rouge">PATH</code>. You can check the path using <code class="language-plaintext highlighter-rouge">which ruff</code> command.</p>

<p>If you want to uninstall a tool such as <code class="language-plaintext highlighter-rouge">ruff</code> installed using <code class="language-plaintext highlighter-rouge">uv</code>, you can run the command below:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv tool uninstall ruff
</code></pre></div></div>
<p>You can list the tools installed in your machine using <code class="language-plaintext highlighter-rouge">uv</code> using the command:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv tool list
</code></pre></div></div>
<p>You can also upgrade all of your tools at once using the following command:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv tool upgrade <span class="nt">--all</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">uv</code> can also use tools like <code class="language-plaintext highlighter-rouge">ruff</code> without installing it permanently on your device. If you want to use tools for some of your project only a few times, <code class="language-plaintext highlighter-rouge">uv</code> will make your life easier. You can run the following command to run <code class="language-plaintext highlighter-rouge">ruff check</code> command without installing <code class="language-plaintext highlighter-rouge">ruff</code>:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv tool run ruff check
</code></pre></div></div>
<p>Alternatively, you can use the short command:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uvx ruff check
</code></pre></div></div>

<p>That’s all for this blog. Although <code class="language-plaintext highlighter-rouge">uv</code> has a lot of functionalities which weren’t covered in this blog, I’ll try to add them as I become more familiar with the other functionalities.</p>]]></content><author><name></name></author><category term="tutorial" /><category term="python" /><summary type="html"><![CDATA[Tutorial Post on Python Project Setup Using UV]]></summary></entry><entry><title type="html">Non-Cartesian MR Image Reconstruction</title><link href="https://sabbir-ahmed12.github.io/blog/2025/non-cartesian-mr-image-reconstruction/" rel="alternate" type="text/html" title="Non-Cartesian MR Image Reconstruction" /><published>2025-01-30T13:24:00+00:00</published><updated>2025-01-30T13:24:00+00:00</updated><id>https://sabbir-ahmed12.github.io/blog/2025/non-cartesian-mr-image-reconstruction</id><content type="html" xml:base="https://sabbir-ahmed12.github.io/blog/2025/non-cartesian-mr-image-reconstruction/"><![CDATA[<div
  class="jupyter-notebook"
  style="position: relative; width: 100%; margin: 0 auto;">
  <div class="jupyter-notebook-iframe-container">
    <iframe
      src="/assets/jupyter/non_cartesian_MR_recon.ipynb.html"
      style="position: absolute; top: 0; left: 0; border-style: none;"
      width="100%"
      height="100%"
      onload="this.parentElement.style.paddingBottom = (this.contentWindow.document.documentElement.scrollHeight + 10) + 'px'"></iframe>
  </div>
</div>]]></content><author><name></name></author><category term="tutorial" /><category term="linux" /><summary type="html"><![CDATA[Tutorial post on non-cartesian MR image reconstruction.]]></summary></entry><entry><title type="html">Learning Linux from Basics to Advanced</title><link href="https://sabbir-ahmed12.github.io/blog/2024/learning-linux-from-basics-to-advanced/" rel="alternate" type="text/html" title="Learning Linux from Basics to Advanced" /><published>2024-07-14T15:03:00+00:00</published><updated>2024-07-14T15:03:00+00:00</updated><id>https://sabbir-ahmed12.github.io/blog/2024/learning-linux-from-basics-to-advanced</id><content type="html" xml:base="https://sabbir-ahmed12.github.io/blog/2024/learning-linux-from-basics-to-advanced/"><![CDATA[<p>Learning Linux is one of the most valuable skills in the tech industry. It helps doing things faster and more efficiently. Many of the world’s powerful servers and supercomputers run on Linux.</p>

<p>In this post, we’ll learn the basics of Linux command line, and then transition to more advanced topics like shell scripting and system administration.</p>

<p><strong>Important Note:</strong> All examples of this post are demonstrated in Ubuntu 22.04 LTS. This blog was written assuming that you’ve a Linux OS already setup for you.</p>

<h2 id="part-1-introduction-to-linux">Part 1: Introduction to Linux</h2>

<h3 id="11-getting-started-with-linux">1.1. Getting Started with Linux</h3>

<h4 id="what-is-linux">What is Linux?</h4>

<p>Linux is an open-source operating system that is based on the Unix Operating System (OS). It was created by Linus Torvalds in 1991. Open source means that the source code of the operating system is available to the public. This allows you to modify the original code, customize it, and distribute the new operating system to potential users.</p>

<h4 id="why-should-you-learn-linux">Why should you learn linux?</h4>

<p>In today’s data center landscape, Linux and Microsoft Windows stand out as the primary contenders, with Linux having a major share.</p>

<p>Some of the compelling reasons to learn Linux are:</p>

<ul>
  <li>
    <p>Given the prevalence of Linux hosting, there is a high chance that your application will be hosted on Linux. So, learning Linux as a developer becomes increasingly valuable.</p>
  </li>
  <li>
    <p>With cloud computing becoming the norm, chances are high that your cloud instance will rely on Linux.</p>
  </li>
  <li>
    <p>Linux serves as the foundation for many operating systems for the Internet of Things (IoT) and mobile applications.</p>
  </li>
  <li>
    <p>Most of the commands you’ll learn can also used in MacOS. Moreover, knowledge of Linux will open many opportunities for you.</p>
  </li>
</ul>

<h4 id="what-is-a-linux-kernel">What is a Linux Kernel?</h4>

<p>The kernel is the central component of an operating system that manages the computer and its hardware operations. It handles memory operations and CPU time. It acts as a bridge between applications and the hardware-level data processing using inter-process communication and system calls.</p>

<p>The kernel is loaded into memory first when an operating system starts and remains there until the system shuts down. It is responsible for tasks like disk management, task management, and memory management.</p>

<div class="row mt-3">
    <div class="col-sm mt-3 mt-md-0">
        <figure>

  <picture>
    

    <!-- Fallback to the original file -->
    <img src="/assets/posts_img/2024_07_14_learning_linux/kernel_layout.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();" />

  </picture>

</figure>

    </div>
</div>

<p>If you’re curious about learning Linux kernel, you can check the GitHub link [here].(https://github.com/torvalds/linux)</p>

<h4 id="what-is-a-linux-distribution">What is a Linux Distribution?</h4>

<p>By this point, you know that you can re-use the Linux kernel code, modify it, and create a new kernel. You can further combine different utilities and software to create a completely new operating system. A Linux distribution or distro is a version of the Linux operating system that includes the Linux kernel, system utilities, and other software. Being open source, a Linux distribution is a collaborative effort involving multiple independent open-source development communities. So, Linux distributions are different flavors of the Linux OS.</p>

<p><strong><em>What does it mean that a distribution is derived?</em></strong>
When you say that a distribution is “derived” from another, the newer distro is built upon the base or foundation of the original distro. This derivation can include using the same package management system (more on this later), kernel version, and sometimes the same configuration tools. Today, there are thousands of Linux distributions to choose from, offering differing goals and criteria for selecting and supporting the software provided by their distribution.</p>

<p>Distributions vary from one to the other, but they generally have several common characteristics:</p>

<ul>
  <li>A distribution consists of a Linux kernel.</li>
  <li>It supports user space programs.</li>
  <li>A distribution may be small and single-purpose or include thousands of open-source programs.</li>
  <li>Some means of installing and updating the distribution and its components should be provided.</li>
</ul>

<p>If you view the <a href="https://upload.wikimedia.org/wikipedia/commons/1/1b/Linux_Distribution_Timeline.svg">Linux Distributions Timeline</a>, you’ll see two major distros: Slackware and Debian. Several distributions are derived from them. For example, Ubuntu and Kali are derived from Debian.</p>

<p>There are various advantages of derivation. Derived distributions can leverage the stability, security, and large software repositories of the parent distribution.</p>

<p>When building on an existing foundation, developers can drive their focus and effort entirely on the specialized features of the new distribution. Users of derived distributions can benefit from the documentation, community support, and resources already available for the parent distribution.</p>

<p>Some popular Linux distributions are: <code class="language-plaintext highlighter-rouge">Ubuntu</code>, <code class="language-plaintext highlighter-rouge">Linux Mint</code>, <code class="language-plaintext highlighter-rouge">Arch Linux</code>, <code class="language-plaintext highlighter-rouge">Manjaro</code>, <code class="language-plaintext highlighter-rouge">Kali Linux</code> etc.</p>

<h2 id="part-2-introduction-to-terminal-console-shell-and-system-commands">Part 2: Introduction to Terminal, Console, Shell and System Commands</h2>

<h3 id="21-introduction-to-terminal-console-and-shell">2.1. Introduction to Terminal, Console and Shell</h3>

<h4 id="what-is-a-terminal">What is a Terminal?</h4>

<p>A terminal is a text-based user interface that allows users to interact with a computer by entering commands and viewing the results. A terminal can be connected to a local or a remote system, depending on the protocol of the network.</p>

<h4 id="what-is-console">What is Console?</h4>

<p>A console is a special type of terminal that is directly attached to the computer system. It is usually the primary interface for system administration and maintenance. It can be a physical device such as a keyboard and a monitor, or a virtual device such as a serial port or graphical window. A console gives you access to the low-level functions and settings of the computer system such as the BIOS, the boot loader or the kernel.</p>]]></content><author><name></name></author><category term="tutorial" /><category term="linux" /><summary type="html"><![CDATA[Tutorial post on linux operating system.]]></summary></entry></feed>